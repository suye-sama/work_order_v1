# 轻量化售后工单系统

## 项目简介

传统售后流程中，工程师完成故障处理仍需手工整理排查过程、命令与日志并补填工单，存在耗时长、格式不统一、关键信息遗漏、历史经验难以复用等问题。本项目面向售后运维团队，基于大模型与 LangGraph 工作流，将售后工单从“人工记录”升级为“日志自动解析、工单智能生成、运维知识沉淀”的闭环流程。

## 核心能力

- **日志解析**：基于 LangGraph 编排日志清洗、命令识别、阶段分割与时间线输出；LLM 不可用时自动降级为规则解析。
- **工单生成**：LangGraph 状态图串联故障现象提取、排查过程梳理、多专家视角并行生成、汇总合成、质量校验与带反馈重试；数据不足时走降级生成路径。
- **知识提取与检索**：从工单中提取四段式知识（问题描述、根因、故障现象、解决方案），存入 ChromaDB，提供语义相似召回、关键词搜索与分类筛选，并做相似内容去重。
- **故障深度分析**：检索相似案例后进行对比分析、风险评估，采用“质疑方—回应方”辩论循环修正结论，输出改进建议。
- **工单管理**：FastAPI 提供工单、客户、工作台、报表、知识库等 REST API；Vue3 管理端支持登录、工单流转、报表可视化和知识库操作。

## 项目结果

- 交付包含工作台、工单中心、客户管理、报表中心、知识库的售后工单管理系统，覆盖工单全生命周期流转与知识复用。
- 打通“日志解析 → 工单生成 → 人工确认 → 知识沉淀 → 相似案例检索”的核心链路，主要页面和业务接口均可正常运行。
- 内置示例数据，默认账号 `admin / admin123` 可直接体验；短日志无 API Key 时可通过规则降级解析，配置大模型 API Key 后启用完整 LLM 分析能力。

## 技术栈

- 后端：Python、FastAPI、SQLAlchemy、Pydantic
- AI 服务：Flask、LangChain、LangGraph、ChromaDB
- 前端：Vue3、Element Plus、ECharts、Vite
- 数据存储：SQLite（本地开发）、PostgreSQL（可选）
- 部署：Docker Compose

## 项目结构

```
work_order_v1/
├── backend/       # FastAPI 主业务服务
├── agent/         # Flask + LangGraph 智能体服务
├── frontend/      # Vue3 管理端
├── README.md      # 项目详细说明
├── 开发文档.md
├── 项目梳理.md
└── docker-compose.yml
```

## 运行说明

详细启动方式见 `README.md` 的「快速开始」章节。默认登录账号为 `admin / admin123`；大模型能力需配置 API Key，未配置时日志解析等流程可走规则降级。
