# 开发文档

---

## 项目概述

### 项目背景

随着公司私有化部署产品规模不断扩大，售后技术支持工作逐渐成为保障客户业务稳定运行的重要组成部分。当前售后服务主要面向教育信息化、企业管理系统等私有化部署场景，工程师通过 VPN、SSH、堡垒机、向日葵、ToDesk 等远程运维工具进入客户环境，对服务器、数据库、应用程序及相关业务系统进行故障排查、问题修复和系统维护。

现阶段售后服务流程主要依赖人工完成，从客户报障到工单归档均缺乏自动化支撑。工程师在完成技术处理后，需要再次整理排查过程、执行命令、问题原因及解决方案，并手动录入售后工单系统进行归档。

现有售后业务流程如下图所示：

```text
客户反馈问题
（电话、微信、微信群等）
           │
           ▼
    售后人员受理问题
           │
           ▼
  远程连接客户环境
(VPN/SSH/堡垒机/向日葵等)
           │
           ▼
    故障排查与问题处理
           │
           ▼
   人工整理处理过程
           │
           ▼
 手动填写售后工单并归档
```

随着客户数量不断增加，售后工单数量持续增长，传统依赖人工记录的方式已难以满足售后管理效率、知识沉淀和数据分析等方面的需求。因此，需要建设一套轻量化、智能化的售后工单管理系统，实现售后处理过程自动记录、工单自动生成和知识资产沉淀，提高售后服务效率和管理水平。

---

### 业务现状

目前售后服务主要围绕远程运维展开，其典型工作流程如下：

1. 客户通过电话、微信、微信群等渠道反馈故障。
2. 售后人员确认问题后创建售后任务。
3. 工程师通过 VPN、SSH、堡垒机、向日葵、ToDesk 等工具远程登录客户环境。
4. 查看系统日志、数据库、服务器运行状态，执行相关诊断及修复操作。
5. 问题解决后，人工整理处理过程并填写售后工单。
6. 工单归档，用于后续查询及统计。

整个过程中，大量关键技术信息（如终端操作记录、日志分析过程、配置修改记录等）未能自动保存，仅依赖工程师事后回忆填写工单，导致大量技术经验无法沉淀。

---

### 现有业务痛点

当前售后模式以人工记录为主，在业务规模不断扩大的背景下逐渐暴露出以下问题。

#### 工单记录效率低

售后处理结束后，工程师需要重新整理整个处理过程，包括：

* 故障现象
* 排查过程
* 执行命令
* 日志分析
* 解决方案
* 根因分析
* 后续建议

上述内容均需人工编写，耗时较长，占用了大量技术人员的工作时间。

---

#### 工单质量不统一

由于不同工程师记录习惯存在差异，工单内容存在以下问题：

* 描述格式不统一；
* 内容详略不一致；
* 排查过程缺失；
* 根因分析不完整；
* 工单质量依赖个人经验。

导致后续查询、统计及经验复盘困难。

---

#### 技术知识无法沉淀

售后过程中最具价值的信息主要包括：

* SSH 操作记录；
* 系统日志分析；
* 数据库处理过程；
* 故障定位思路；
* 服务恢复方案；
* 配置调整记录。

由于缺乏自动采集机制，这些技术经验难以形成结构化知识，导致：

* 新员工学习成本高；
* 相同问题重复排查；
* 企业技术资产无法积累。

---

#### 数据统计能力不足

当前工单仅作为归档记录使用，缺少统一的数据分析能力，例如：

* 工单数量统计；
* 问题类型分析；
* 工程师工作量统计；
* 客户故障趋势分析；
* 服务质量分析。

管理人员无法基于数据进行业务决策。

---

#### 工程师填写积极性较低

售后工程师的核心工作是故障诊断与问题修复，而非文档整理。

由于工单填写属于重复性工作，容易出现以下情况：

* 工单延迟填写；
* 内容简单敷衍；
* 关键步骤遗漏；
* 无法真实反映处理过程。

进一步影响工单质量和知识沉淀效果。

---

### 项目建设目标

本项目旨在建设一套**轻量化、自动化、智能化**的售后工单管理系统，实现售后全过程数字化管理，并借助 AI 技术完成工单生成与知识沉淀。

总体建设目标如下：

#### 自动记录售后处理过程

自动采集工程师在售后处理过程中的关键操作，包括：

* SSH 命令执行记录；
* 堡垒机会话日志；
* 系统运行日志；
* 工程师补充说明；
* 客户环境信息。

最大程度减少人工记录工作。

---

#### 自动生成标准化工单

基于采集到的售后数据，通过 AI 自动整理生成标准工单，统一工单结构，包括：

* 故障现象
* 排查过程
* 解决方案
* 根因分析
* 工单摘要
* 时间线

工程师仅需进行审核确认即可完成工单提交。

---

#### 智能总结故障信息

利用大语言模型（LLM）对售后处理数据进行分析，自动提取：

* 故障影响范围；
* 关键排查步骤；
* 核心解决方案；
* 根因分析；
* 后续建议。

提升工单质量和可读性。

---

#### 自动沉淀知识资产

系统在工单完成后自动提取故障知识，形成标准化知识条目，并建立企业知识库，实现：

* 历史问题检索；
* 相似故障推荐；
* 知识复用；
* 新员工培训；
* 企业技术资产积累。

---

### 建设原则

为保证系统具备良好的可扩展性、易维护性和可持续发展能力，本项目遵循以下建设原则。

#### 轻量化原则

采用前后端分离架构，系统部署简单，组件数量少，能够快速上线并降低运维成本。

---

#### 自动化原则

尽可能自动完成售后数据采集、工单生成、消息通知及知识沉淀，减少人工参与，提高工作效率。

---

#### 智能化原则

引入基于大语言模型（LLM）的 Agent 智能体，实现故障分析、工单生成及知识提取等智能化能力，提升售后处理质量。

---

#### 标准化原则

统一工单模板、问题分类、处理流程及知识结构，确保工单内容规范一致，为后续统计分析和知识管理提供基础。

---

#### 可扩展原则

系统采用模块化设计，各功能模块通过标准接口解耦，支持后续扩展知识库、智能推荐、多 Agent 协同等能力。

---

#### 安全可追溯原则

系统完整记录工单状态变化、用户操作日志、处理时间线及关键数据，实现全过程可追溯，满足售后审计和问题复盘需求。

---

### 系统建设范围

本项目一期主要建设以下核心功能模块：

| 模块               | 建设内容                                                 |
| ------------------ | -------------------------------------------------------- |
| 工作台             | 我的待办、进行中、本周完成、快速创建工单                 |
| 工单中心           | 工单全生命周期管理、工单详情、状态流转、处理时间线       |
| 客户管理           | 客户信息、联系人管理、工单关联客户                       |
| 报表中心           | 工单统计、工作量分析、Excel 导出                         |
| 消息中心           | 系统通知、状态变更推送、消息中台对接                     |
| 系统管理           | 用户管理、角色权限、问题分类、消息配置、数据备份         |
| Agent 智能服务     | 售后过程解析、工单自动生成、故障总结、知识提取           |
| 数据采集模块       | 堡垒机会话采集、终端日志采集、Webhook 接入、人工补录     |
| 知识库（二期预留） | 数据模型、接口及页面框架预留，支持后续知识沉淀与智能检索 |

---

### 预期建设成果

项目建成后，将实现售后业务由“人工记录”向“自动采集、智能生成、知识沉淀”的转变，形成完整的数据闭环：

```text
客户报障
    │
    ▼
工程师远程处理
    │
    ▼
自动采集操作数据
    │
    ▼
Agent 智能分析
    │
    ▼
自动生成标准工单
    │
    ▼
工程师审核确认
    │
    ▼
工单归档
    │
    ▼
知识库沉淀
    │
    ▼
历史经验复用与智能推荐
```

系统最终将实现以下价值：

* 提升售后工单编写效率，减少重复性工作；
* 统一工单格式，提高工单质量；
* 自动沉淀企业售后知识资产，增强知识复用能力；
* 提供完整的数据统计与分析能力，支撑管理决策；
* 为后续构建 AI 驱动的智能售后平台奠定基础。





------

## 总体架构设计

### 架构设计目标

本系统定位为一套**轻量化、智能化、可扩展**的售后工单管理平台，围绕"自动采集、智能分析、结构化沉淀"三条主线进行设计，在保证系统稳定性的同时，引入 AI Agent 能力，实现售后处理全过程数字化管理。

总体架构设计遵循以下目标：

-   **业务与 AI 解耦**：将传统业务逻辑与 AI 推理能力拆分为独立服务，降低系统耦合度。
-   **前后端分离**：采用 Vue3 + FastAPI 构建现代化 Web 应用，提高开发效率和用户体验。
-   **异步处理**：AI 分析、消息推送、报表生成等耗时任务采用异步执行，避免阻塞业务请求。
-   **模块化设计**：各业务模块通过标准 REST API 通信，便于后续扩展。
-   **知识持续沉淀**：为二期知识库建设预留数据结构及向量检索能力。

------

### 总体架构

系统采用**前后端分离 + 微服务 + AI Agent**架构，整体分为五层：

-   表现层（Presentation）
-   业务层（Business）
-   Agent 智能层（AI Agent）
-   数据层（Data）
-   基础设施层（Infrastructure）

系统总体架构如下：

```text
                              ┌────────────────────────────┐
                              │        Web 浏览器          │
                              │   Vue3 + Element Plus      │
                              └─────────────┬──────────────┘
                                            │
                                   HTTP / WebSocket
                                            │
                              ┌─────────────▼──────────────┐
                              │      FastAPI 主业务服务     │
                              │                            │
                              │ 工单 │ 客户 │ 用户 │ 报表 │
                              │ 权限 │ 消息 │ 系统管理     │
                              └──────┬─────────────┬───────┘
                                     │             │
                             REST API│             │Redis
                                     │             │
                      ┌──────────────▼───┐   ┌────▼─────┐
                      │ Flask Agent 服务 │   │ Celery   │
                      │                  │   │ 异步任务 │
                      │ 工单生成Agent    │   └────┬─────┘
                      │ 总结Agent        │        │
                      │ 知识Agent        │        │
                      └─────────┬────────┘        │
                                │                 │
                        LangChain/LlamaIndex      │
                                │                 │
                         ┌──────▼─────────┐       │
                         │     LLM模型     │       │
                         └─────────────────┘       │
                                                   │
     ┌─────────────────────────────────────────────┴────────────────────┐
     │                                                                  │
┌────▼────┐         ┌─────────┐        ┌────────────┐       ┌────────────┐
│PostgreSQL│         │ Redis   │        │ Chroma DB │       │ 文件存储    │
│业务数据   │         │缓存队列 │        │向量知识库 │       │日志/附件    │
└──────────┘         └─────────┘        └────────────┘       └────────────┘
```

------

### 系统组成

整个系统由六个核心部分组成。

#### 前端应用层

前端采用 **Vue3 + Element Plus** 实现后台管理系统。

主要职责包括：

-   登录认证；
-   工作台展示；
-   工单管理；
-   客户管理；
-   报表展示；
-   系统配置；
-   WebSocket 实时消息接收；
-   AI 工单生成结果展示。

采用 SPA（Single Page Application）模式部署，最终编译为静态资源，由 Nginx 提供访问。

#### FastAPI 主业务服务

FastAPI 是整个系统的业务核心，负责所有传统业务逻辑。

主要职责如下：

| 模块       | 职责                |
| ---------- | ------------------- |
| 用户管理   | 登录、认证、权限    |
| 工单中心   | 工单生命周期管理    |
| 客户管理   | 客户及联系人维护    |
| 报表中心   | 数据统计分析        |
| 消息中心   | WebSocket、消息推送 |
| 系统管理   | 参数配置、数据备份  |
| Agent 调度 | 调用 Flask Agent    |

FastAPI 对外统一提供 REST API。

------

#### Agent 智能服务

Agent 服务采用 Flask 独立部署。

主要原因包括：

-   AI 调用耗时较长；
-   Prompt 调试频繁；
-   工具链复杂；
-   后期方便横向扩容。

Agent 服务主要负责：

-   售后过程解析；
-   工单自动生成；
-   故障总结；
-   根因分析；
-   知识提取；
-   相似案例推荐。

整个 Agent 采用 LangChain 或 LlamaIndex 实现工具链管理，并支持接入 GPT、Qwen、ChatGLM 等大语言模型。

------

#### 数据存储层

系统采用多种数据存储方式，根据数据特点分别保存。

**PostgreSQL**

保存结构化业务数据：

-   用户
-   工单
-   客户
-   联系人
-   消息
-   日志
-   配置

------

**Redis**

主要用于：

-   登录 Token
-   热点缓存
-   WebSocket 会话
-   Celery Broker
-   AI 任务队列

------

**Chroma 向量数据库**

主要用于知识库。

保存：

-   工单 Embedding
-   故障知识
-   解决方案
-   历史经验

用于后续：

-   相似工单推荐
-   RAG 检索
-   Agent 知识增强

------

**文件存储**

保存：

-   SSH 日志
-   堡垒机会话
-   操作录像
-   工单附件
-   Excel 导出文件

一期可以采用本地目录，后续可扩展至 MinIO 或对象存储。

------

#### 消息与异步任务

系统采用 **Redis + Celery** 实现异步处理。

主要异步任务包括：

-   AI 工单生成；
-   Agent 总结；
-   Excel 导出；
-   消息推送；
-   数据备份；
-   知识库生成。

避免用户请求长时间等待。

------

#### 第三方系统集成

系统预留标准接口，可与企业现有平台集成。

包括：

-   钉钉 OAuth 登录；
-   钉钉机器人；
-   企业微信；
-   JumpServer；
-   Teleport；
-   云堡垒机；
-   消息中台；
-   企业 Wiki；
-   LDAP。

------

### 系统逻辑架构

系统采用典型分层架构。

```text
┌──────────────────────────────────────┐
│            Presentation              │
│ Vue3、Element Plus、浏览器           │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│             API Gateway              │
│ FastAPI REST API                     │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│           Business Service           │
│ 工单│客户│报表│权限│通知│系统配置      │
└──────────────────────────────────────┘
                 │
         ┌───────┴────────┐
         ▼                ▼
┌────────────────┐  ┌────────────────┐
│ Agent Service  │  │ Celery Worker  │
└────────────────┘  └────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│      PostgreSQL / Redis / Chroma     │
└──────────────────────────────────────┘
```

各层职责明确，通过 REST API 或消息队列进行通信，实现业务与 AI 服务解耦。

------

### 核心业务流程

售后工单从创建到知识沉淀形成完整闭环。

```text
客户报障
    │
    ▼
创建工单
    │
    ▼
工程师处理故障
    │
    ▼
采集操作日志
    │
    ▼
保存工单草稿
    │
    ▼
调用 Agent
    │
    ▼
生成标准工单
    │
    ▼
工程师确认
    │
    ▼
工单完成
    │
    ▼
生成知识条目
    │
    ▼
向量化存储
```

整个流程中，FastAPI 负责业务流转，Agent 负责智能分析，数据库负责持久化，形成闭环。

------

### 技术架构选型

| 层次       | 技术方案               | 选型说明                          |
| ---------- | ---------------------- | --------------------------------- |
| 前端框架   | Vue3 + Element Plus    | 组件丰富，开发效率高              |
| Web 服务   | Nginx                  | 静态资源部署、反向代理            |
| 后端框架   | FastAPI                | 异步高性能，适合 REST API         |
| AI 服务    | Flask                  | 轻量独立部署，便于 Agent 调试     |
| ORM        | SQLAlchemy             | Python 主流 ORM 框架              |
| 数据库     | PostgreSQL             | 稳定、支持 JSONB、全文检索        |
| 缓存       | Redis                  | 缓存、队列、会话管理              |
| 异步任务   | Celery                 | 处理 AI、导出、推送等耗时任务     |
| 消息通信   | WebSocket              | 实时通知工单状态变化              |
| AI 框架    | LangChain / LlamaIndex | Agent 编排、工具调用、Prompt 管理 |
| 大模型     | GPT、Qwen、ChatGLM     | 工单生成、总结、知识提取          |
| 向量数据库 | Chroma（一期）         | 轻量部署，支持语义检索            |
| 文件存储   | 本地文件系统（一期）   | 保存日志、附件、导出文件          |
| 容器化     | Docker Compose         | 一键部署，降低运维复杂度          |

------

### 核心设计思想

系统围绕"自动采集、智能生成、知识沉淀"三个核心能力构建。

#### 自动采集

通过对接堡垒机、SSH 会话日志、终端录制工具及人工补录，实现售后处理过程自动记录，减少人工整理工作。

------

#### 智能生成

基于 Agent 调用大语言模型，对处理过程进行分析和结构化总结，自动生成符合企业规范的标准工单，提升工单质量和一致性。

------

#### 知识沉淀

工单完成后，系统自动提取故障现象、根因及解决方案，生成知识条目并向量化存储，为后续相似问题推荐和智能检索提供支撑。

------

#### 解耦设计

FastAPI 与 Agent 服务通过 REST API 通信，业务处理与 AI 推理相互独立，便于后续升级模型、扩展工具链或替换 AI 框架，而无需影响核心业务系统。

------

###  本章小结

本章从整体架构层面对系统进行了设计，明确了系统采用**前后端分离 + 微服务 + AI Agent**的总体架构，定义了各层职责、核心组件及技术选型。通过 FastAPI 承载业务能力、Flask Agent 提供智能分析、Redis 与 Celery 支撑异步任务、PostgreSQL 与 Chroma 分别负责结构化数据和知识向量存储，形成了稳定、高效且易于扩展的技术架构，为后续功能设计、数据模型设计及接口设计奠定了基础。





------

## 3 功能设计

### 功能模块概述

轻量化售后工单系统采用模块化设计，以售后工单全生命周期管理为核心，围绕工程师日常工作流程构建业务功能，覆盖工单创建、处理、归档、统计分析及知识沉淀等全过程。

系统一期共规划 **7 个核心业务模块**和 **1 个二期预留模块**。

| 一级模块       | 二级功能                                       | 说明             |
| -------------- | ---------------------------------------------- | ---------------- |
| 工作台         | 我的待办、我的进行中、本周已完成、快速创建工单 | 工程师工作首页   |
| 工单中心       | 工单列表、工单详情、状态流转、时间线           | 工单生命周期管理 |
| 客户信息       | 客户管理、联系人管理                           | 客户基础资料维护 |
| 报表中心       | 数据统计、工作量统计、Excel导出                | 数据分析         |
| 消息中心       | 系统通知、消息推送                             | 实时通知         |
| 系统管理       | 用户、角色、权限、配置、备份                   | 系统基础管理     |
| Agent智能中心  | 自动生成工单、智能总结、知识提取               | AI能力中心       |
| 知识库（二期） | 知识管理、知识检索                             | 一期预留         |

系统功能结构如下：

```text
轻量化售后工单系统
│
├── 工作台
├── 工单中心
├── 客户管理
├── 报表中心
├── 消息中心
├── 系统管理
├── Agent智能中心
└── 知识库（二期）
```

------

### 工作台模块

#### 模块概述

工作台是工程师登录后的首页，聚焦个人工作任务，提供待办工单、进行中工单、已完成工单及快速创建入口，提高日常工作效率。

主要功能包括：

-   我的待办
-   我的进行中
-   本周已完成
-   快速创建工单

------

#### 我的待办

展示当前待处理工单。

显示信息包括：

| 字段     | 说明      |
| -------- | --------- |
| 工单编号 | Ticket No |
| 工单标题 | 问题描述  |
| 客户名称 | 所属客户  |
| 优先级   | 高/中/低  |
| 创建时间 | 创建日期  |
| 截止时间 | SLA时间   |
| 当前状态 | 待处理    |
| 操作     | 开始处理  |

支持：

-   排序
-   快速搜索
-   一键开始处理

------

#### 我的进行中

展示当前工程师正在处理的工单。

包括：

-   当前处理阶段
-   已处理时长
-   最新更新时间
-   当前处理人
-   Agent分析状态

支持：

-   查看详情
-   提交处理结果
-   调用Agent生成总结

------

#### 本周已完成

统计最近七天已完成工单。

展示：

-   工单数量
-   完成时间
-   客户名称
-   AI评分（预留）
-   查看详情

便于工程师工作复盘。

------

#### 快速创建工单

适用于电话报修、微信报障等快速录入场景。

仅填写：

-   客户
-   标题
-   问题描述
-   优先级

系统自动补充：

-   创建人
-   创建时间
-   默认状态
-   工单编号

创建完成后进入待处理状态。

------

### 工单中心

#### 模块概述

工单中心是整个系统的核心模块，实现工单从创建到完成的全过程管理。

主要包括：

-   工单创建
-   工单查询
-   工单处理
-   工单流转
-   工单归档
-   AI生成工单

------

#### 工单生命周期

系统采用固定状态机控制工单流转。

```text
新建
 │
 ▼
待处理
 │
 ▼
处理中
 │
 ▼
待确认
 │
 ▼
已完成
 │
 ▼
已归档
```

各状态说明：

| 状态   | 描述                      |
| ------ | ------------------------- |
| 新建   | 工单创建成功              |
| 待处理 | 等待工程师受理            |
| 处理中 | 工程师正在处理            |
| 待确认 | Agent已生成工单，等待确认 |
| 已完成 | 工单正式完成              |
| 已归档 | 数据进入历史档案          |

状态流转由系统统一控制，不允许任意跳转。

------

#### 工单列表

支持多个维度查询：

-   全部工单
-   我的工单
-   我创建的工单

支持筛选：

-   工单编号
-   客户名称
-   问题类型
-   状态
-   创建时间
-   完成时间
-   工程师
-   优先级

支持：

-   分页
-   排序
-   导出Excel

------

#### 工单详情

工单详情包括多个区域。

**基本信息**

包括：

-   工单编号
-   标题
-   客户
-   联系人
-   创建时间
-   指派人
-   当前状态

------

**故障信息**

包括：

-   故障描述
-   故障现象
-   影响范围
-   问题分类

------

**处理过程**

展示：

-   操作记录
-   SSH命令
-   Agent摘要
-   工程师备注
-   附件

------

**时间线**

采用时间轴展示全过程。

```text
创建工单

↓

开始处理

↓

采集日志

↓

Agent生成总结

↓

工程师确认

↓

工单完成
```

------

**操作日志**

记录所有操作：

-   创建
-   修改
-   指派
-   删除
-   状态变更
-   AI生成

满足审计要求。

------

#### 工单处理流程

```text
创建工单
    │
    ▼
工程师受理
    │
    ▼
处理问题
    │
    ▼
采集日志
    │
    ▼
Agent总结
    │
    ▼
人工确认
    │
    ▼
完成工单
```

------

### 客户信息模块

#### 模块概述

统一维护客户资料，为工单自动关联客户信息提供基础数据。

包括：

-   客户管理
-   联系人管理

------

#### 客户管理

维护：

| 字段     | 说明          |
| -------- | ------------- |
| 客户名称 | 学校/单位     |
| 所属地区 | 地区          |
| 产品版本 | 软件版本      |
| 部署方式 | 本地/云端     |
| 部署环境 | Windows/Linux |
| 创建时间 | 创建日期      |

支持：

-   新增
-   修改
-   删除
-   查询

------

#### 联系人管理

每个客户支持多个联系人。

包括：

-   姓名
-   手机
-   微信
-   邮箱
-   职务

工单可直接关联联系人。

------

#### 客户关联工单

创建工单时：

选择客户

↓

自动带出：

-   联系人
-   产品版本
-   部署方式
-   环境信息

减少重复录入。

------

### 报表中心

#### 模块概述

报表中心用于统计售后数据，为管理层提供决策依据。

------

#### 基础统计

统计内容：

-   工单总数
-   已完成
-   待处理
-   处理中
-   超时工单

图表展示：

-   饼图
-   柱状图
-   趋势图

------

#### 问题分类统计

按照问题类型统计：

-   系统故障
-   数据库异常
-   功能BUG
-   操作咨询
-   网络异常

展示占比。

------

#### 工作量统计

按：

-   日
-   周
-   月

统计：

-   工单数量
-   完成率
-   平均处理时长
-   超时数量

支持工程师排名。

------

#### 数据导出

支持：

-   Excel
-   CSV（预留）

导出内容：

-   工单
-   客户
-   报表

采用后台异步导出。

------

### 消息中心

#### 模块概述

负责工单动态通知。

------

#### 系统通知

包括：

-   工单创建
-   指派
-   状态变化
-   Agent完成
-   超时提醒

采用WebSocket实时推送。

------

#### 外部通知

支持：

-   钉钉机器人
-   企业微信（预留）
-   消息中台

采用Webhook发送。

------

### 系统管理

#### 用户管理

支持：

-   钉钉登录
-   用户同步
-   用户管理

字段：

-   姓名
-   手机
-   邮箱
-   部门
-   角色

------

#### RBAC权限管理

采用角色权限模型。

角色：

-   超级管理员
-   运维管理员
-   售后工程师
-   查看人员

权限包括：

-   菜单权限
-   页面权限
-   API权限
-   数据权限

------

#### 问题分类管理

维护：

-   一级分类
-   二级分类

例如：

系统故障

├── 登录异常

├── 服务异常

├── 数据异常

------

#### 消息配置

配置：

-   Webhook
-   钉钉机器人
-   推送规则

支持动态启停。

------

#### 数据备份

支持：

-   数据库备份
-   文件备份
-   定时备份

采用定时任务执行。

------

### Agent 智能中心

#### 模块概述

Agent是本系统智能能力的核心。

主要能力：

-   自动解析日志
-   自动总结
-   自动生成工单
-   自动知识提取

Agent独立部署，通过REST API调用。

------

#### 自动工单生成

输入：

-   SSH日志
-   工程师备注
-   客户信息

输出：

-   故障现象
-   排查步骤
-   根因分析
-   解决方案

生成标准JSON。

------

#### 智能总结

生成：

-   摘要
-   时间线
-   工单正文

减少人工填写。

------

#### 知识提取

完成工单后：

提取：

```
现象

↓

根因

↓

解决方案

↓

知识条目

↓

Embedding

↓

向量数据库
```

供后续RAG检索使用。

------

### 知识库模块（二期预留）

一期预留：

-   数据表
-   API
-   页面
-   向量字段

二期实现：

-   知识编辑
-   智能检索
-   相似案例推荐
-   RAG问答
-   AI辅助排障

------

### 功能模块关系

各模块之间通过工单数据进行关联，形成统一业务闭环。

```text
                     工作台
                        │
                        ▼
                    工单中心
         ┌──────────┼──────────┐
         ▼          ▼          ▼
     客户管理    Agent中心    消息中心
         │          │          │
         └──────┬───┴──────────┘
                ▼
            工单生命周期
                │
                ▼
             报表中心
                │
                ▼
            知识库（二期）
```

整个系统以**工单中心**为核心，通过客户管理提供基础数据，Agent 中心提供智能能力，消息中心保障流程协同，报表中心提供统计分析，并最终将工单经验沉淀至知识库，形成从售后处理到知识复用的完整业务闭环。



------

## Agent 智能体设计

### 概述

Agent（智能体）是轻量化售后工单系统的核心智能能力，负责对售后处理过程中产生的各类数据进行采集、分析、理解和结构化处理，实现工单自动生成、故障总结、知识沉淀及智能推荐等功能。

与传统工单系统仅负责数据管理不同，本系统引入大语言模型（LLM）和 Agent 技术，将工程师在售后处理过程中的操作日志、故障信息及客户环境信息转化为结构化工单和企业知识资产，实现售后工作由“人工记录”向“智能辅助”的转变。

Agent 采用独立微服务部署方式，与 FastAPI 主业务系统解耦，通过标准 REST API 提供智能分析能力，支持后续模型升级和能力扩展。

------

### Agent 建设目标

Agent 服务围绕售后业务的四项核心目标进行设计。

#### 售后过程自动记录

自动采集工程师在远程维护过程中产生的数据，包括：

-   SSH 操作命令；
-   终端输出内容；
-   堡垒机会话日志；
-   客户环境信息；
-   工程师补充说明；
-   系统运行日志。

通过对上述数据进行实时分析，自动生成处理时间线和阶段性摘要。

------

#### 工单自动生成

售后完成后，Agent 自动汇总所有处理数据，并生成标准化工单，包括：

-   故障现象
-   影响范围
-   排查过程
-   解决方案
-   根因分析
-   处理总结

工程师仅需审核确认即可完成工单提交。

------

#### 故障智能总结

利用大语言模型分析整个售后过程，自动提炼：

-   核心故障原因；
-   排查思路；
-   最佳实践；
-   风险提示；
-   后续建议。

提高工单质量和可读性。

------

#### 知识自动沉淀

工单完成后，自动生成标准知识条目，并进行向量化存储，实现：

-   相似问题推荐；
-   知识检索；
-   智能问答（RAG）；
-   新员工培训；
-   企业经验积累。

------

### Agent 总体架构

Agent 服务采用模块化设计，由任务调度、工具调用、Prompt 编排、大模型推理及结果解析等多个组件组成。

整体架构如下：

```text
                        FastAPI
                           │
                           │REST API
                           ▼
                 Flask Agent Service
                           │
     ┌──────────────┬──────────────┬──────────────┐
     ▼              ▼              ▼
 Prompt Engine   Tool Manager   Memory Manager
     │              │              │
     └──────────────┼──────────────┘
                    ▼
             LangChain Agent
                    │
      ┌─────────────┼─────────────┐
      ▼             ▼             ▼
 Customer Tool   Log Tool   Knowledge Tool
      │             │             │
      └─────────────┼─────────────┘
                    ▼
                 LLM模型
                    │
                    ▼
           Structured Output
                    │
                    ▼
             FastAPI 主业务系统
```

各组件职责如下：

| 组件           | 职责                        |
| -------------- | --------------------------- |
| Prompt Engine  | Prompt 模板管理与上下文拼接 |
| Tool Manager   | 工具注册与调用              |
| Memory Manager | 会话上下文管理              |
| LLM            | 自然语言理解与推理          |
| Output Parser  | 输出结构化 JSON             |
| Agent API      | 对外提供统一接口            |

------

### Agent 功能架构

根据售后业务场景，Agent 划分为四个核心智能体。

```text
                Agent Service
                      │
      ┌───────────────┼────────────────┐
      ▼               ▼                ▼
过程采集Agent     工单生成Agent     知识沉淀Agent
                      │
                      ▼
                推荐分析Agent
```

各智能体既可独立执行，也可通过任务链协同完成复杂业务。

------

### 售后过程采集 Agent

#### 功能定位

过程采集 Agent 负责接收工程师售后过程中产生的各类数据，并进行标准化处理。

支持数据来源：

-   堡垒机会话日志；
-   SSH 操作记录；
-   Script 录制日志；
-   Asciinema 日志；
-   WebHook 推送；
-   工程师手工粘贴日志；
-   工程师备注。

------

#### 工作流程

```text
SSH日志
     │
     ▼
Webhook
     │
     ▼
日志解析
     │
     ▼
命令识别
     │
     ▼
阶段总结
     │
     ▼
写入Timeline
```

处理完成后生成：

-   时间节点；
-   执行命令；
-   错误信息；
-   阶段摘要。

------

#### 输出结果

生成统一结构：

```json
{
  "step": 3,
  "time": "2026-01-10 14:30:20",
  "operation": "Restart Service",
  "command": "systemctl restart nginx",
  "summary": "重新启动Nginx服务",
  "result": "服务恢复正常"
}
```

该结构作为工单时间线的数据来源。

------

### 工单生成 Agent

#### 功能定位

工单生成 Agent 是整个系统最核心的智能体。

负责：

-   汇总售后数据；
-   理解故障处理过程；
-   自动生成标准工单；
-   输出结构化 JSON。

------

#### 输入数据

包括：

-   工单基本信息；
-   客户信息；
-   SSH 日志；
-   Agent 摘要；
-   操作时间线；
-   工程师备注；
-   系统日志。

------

#### Prompt 链设计

采用多阶段 Prompt Chain，而非一次性生成。

```text
上下文

↓

故障现象提取

↓

影响范围分析

↓

排查过程整理

↓

根因分析

↓

解决方案总结

↓

输出JSON
```

每一步均可单独优化 Prompt，提高整体生成质量和稳定性。

------

#### 输出模板

Agent 最终输出统一 JSON，例如：

```json
{
  "fault": "登录系统失败",
  "reason": "Redis缓存异常",
  "solution": "重启Redis服务",
  "steps": [
    "...",
    "..."
  ],
  "duration": "45分钟",
  "summary": "Redis缓存服务异常导致登录失败，重启后恢复正常。"
}
```

FastAPI 接收后自动写入工单。

------

### 故障分析 Agent

#### 功能定位

故障分析 Agent 负责对售后处理过程进行深度分析，而不仅仅是总结文字。

主要输出：

-   根因分析；
-   风险分析；
-   影响范围；
-   是否可能再次发生；
-   建议优化方案。

例如：

```text
问题

↓

分析原因

↓

定位根因

↓

评估影响

↓

提出建议
```

分析结果可作为工单附加内容，也可供管理层复盘。

------

### 知识沉淀 Agent

#### 功能定位

工单完成后自动提取知识。

生成标准知识条目。

------

#### 提取内容

主要包括：

-   故障现象
-   根因
-   排查步骤
-   解决方案
-   标签
-   分类
-   产品版本

------

#### 工作流程

```text
完成工单

↓

提取知识

↓

生成知识条目

↓

Embedding

↓

写入Chroma

↓

更新Knowledge表
```

形成完整知识资产。

------

#### 知识结构

知识条目包括：

| 字段      | 说明     |
| --------- | -------- |
| 标题      | 故障标题 |
| 分类      | 问题分类 |
| 标签      | 标签     |
| 摘要      | AI总结   |
| 解决方案  | 标准方案 |
| 原始工单  | 工单ID   |
| Embedding | 向量数据 |

------

### 推荐分析 Agent

#### 功能定位

当工程师创建新工单时，自动推荐相似案例。

流程如下：

```text
输入问题

↓

Embedding

↓

向量检索

↓

相似工单

↓

推荐解决方案
```

推荐内容包括：

-   相似工单；
-   历史处理过程；
-   推荐解决方案；
-   相似度评分。

------

### Agent Tool 设计

为了增强 Agent 的自主决策能力，引入 Tool（工具）机制，使 Agent 在推理过程中能够主动调用外部服务获取信息，而不仅依赖 Prompt。

一期规划以下工具：

| Tool              | 功能             |
| ----------------- | ---------------- |
| Customer Tool     | 查询客户环境信息 |
| Ticket Tool       | 查询历史工单     |
| Knowledge Tool    | 检索知识库       |
| Log Tool          | 查询日志         |
| Config Tool       | 查询系统配置     |
| Search Tool       | 企业文档检索     |
| Notification Tool | 推送分析结果     |

Agent 可根据任务自动选择工具，形成「推理 → 调用工具 → 获取结果 → 再推理」的闭环。

------

### Memory（记忆）设计

为了提升连续分析能力，Agent 引入 Memory 机制保存上下文。

Memory 包括：

| 类型     | 内容           |
| -------- | -------------- |
| 短期记忆 | 当前工单上下文 |
| 长期记忆 | 企业知识库     |
| 会话记忆 | 多轮分析过程   |
| 用户记忆 | 工程师备注     |

Memory 用于：

-   多轮 Prompt；
-   长日志分析；
-   分阶段总结；
-   连续生成工单。

------

### Agent 接口设计

Agent 对外统一提供 REST API。

主要接口如下：

| 接口                          | 功能         |
| ----------------------------- | ------------ |
| POST /agent/log/parse         | 日志解析     |
| POST /agent/ticket/generate   | 工单生成     |
| POST /agent/fault/analyze     | 根因分析     |
| POST /agent/knowledge/extract | 知识提取     |
| POST /agent/search            | 相似工单推荐 |
| POST /agent/summary           | 工单摘要     |

FastAPI 作为唯一调用入口，前端不直接访问 Agent 服务。

------

### Agent 执行时序

完整执行流程如下：

```text
工程师完成工单
        │
        ▼
 FastAPI提交Agent任务
        │
        ▼
 Celery异步执行
        │
        ▼
 Agent读取上下文
        │
        ▼
 调用Tool获取客户信息
        │
        ▼
 LLM分析日志
        │
        ▼
 输出结构化工单
        │
        ▼
 FastAPI保存工单
        │
        ▼
 工程师确认
        │
        ▼
 知识提取
        │
        ▼
 写入向量数据库
```

------

### 本章小结

本章围绕 Agent 智能体进行了整体设计，构建了以 **Flask + LangChain + LLM** 为核心的智能分析架构，明确了过程采集、工单生成、故障分析、知识沉淀和推荐分析五类智能体的职责与协作方式，并设计了 Tool、Memory、Prompt Chain 及统一 API 等关键能力。

通过将 AI 能力独立为 Agent 微服务，系统能够实现售后过程自动记录、标准工单自动生成、故障智能总结和知识资产沉淀，为后续引入 RAG、多 Agent 协同、智能诊断等高级能力提供良好的扩展基础。





------

## 数据采集设计

### 概述

数据采集模块是轻量化售后工单系统的重要组成部分，也是 Agent 智能分析的数据基础。

传统售后工单依赖工程师事后回忆填写，导致大量关键操作过程无法保留。本系统通过对接现有远程运维工具和日志系统，自动采集售后处理全过程的数据，为 Agent 自动生成工单、故障分析和知识沉淀提供完整的数据来源。

数据采集设计遵循以下原则：

-   **尽量利用现有基础设施**：优先对接堡垒机、VPN、SSH 等现有运维体系，不改变工程师工作习惯。
-   **低侵入部署**：避免在客户服务器安装复杂代理，降低实施成本。
-   **多源数据融合**：支持日志、命令、系统信息、人工备注等多种数据来源。
-   **可扩展设计**：预留 WebHook、Agent SDK 等接口，支持未来接入更多数据源。
-   **安全可追溯**：采集的数据仅用于售后分析与工单生成，完整记录采集来源和时间。

------

### 数据采集总体架构

系统采用"多数据源采集 + 数据标准化 + Agent 分析"的架构，将不同来源的数据统一转换为标准格式。

总体流程如下：

```text
                 工程师远程维护
                       │
      ┌────────────────┼────────────────┐
      ▼                ▼                ▼
   SSH终端         堡垒机会话       向日葵/ToDesk
      │                │                │
      └────────────────┼────────────────┘
                       ▼
                数据采集模块
                       │
      ┌────────────────┼────────────────┐
      ▼                ▼                ▼
   日志解析         数据清洗        数据标准化
                       │
                       ▼
                PostgreSQL
                       │
                       ▼
                 Agent 智能分析
                       │
                       ▼
                自动生成标准工单
```

采集模块作为 FastAPI 与 Agent 之间的数据桥梁，为后续智能处理提供统一的数据格式。

------

### 数据来源设计

根据售后业务特点，系统支持以下几类数据来源。

| 数据来源      | 数据内容                 | 采集方式         | 一期支持    |
| ------------- | ------------------------ | ---------------- | ----------- |
| 堡垒机会话    | SSH 命令、回显、文件操作 | WebHook/API      | √           |
| SSH 终端日志  | 命令、输出、错误信息     | Script/Asciinema | √           |
| 系统日志      | 应用日志、系统日志       | 上传/接口        | √           |
| 工程师备注    | 文字说明                 | 页面录入         | √           |
| 客户信息      | 环境、版本、部署方式     | FastAPI 查询     | √           |
| 工单附件      | 图片、日志、压缩包       | 上传             | √           |
| 向日葵/ToDesk | 会话录像                 | 附件关联         | √（仅存储） |
| Web 管理后台  | 操作日志                 | 二期扩展         | ○           |
| 企业监控平台  | 告警信息                 | 二期扩展         | ○           |

其中，**堡垒机会话日志、SSH 终端日志和工程师备注**是一期 Agent 分析的核心数据来源。

------

### 数据采集方案设计

#### 堡垒机会话采集（推荐方案）

功能说明

企业已部署 JumpServer、Teleport、阿里云堡垒机等运维平台时，可直接利用其会话审计能力，无需额外开发录制程序。

堡垒机会自动记录：

-   SSH 登录信息；
-   执行命令；
-   命令输出；
-   文件上传下载；
-   会话开始、结束时间。

实现流程

```text
工程师登录堡垒机
        │
        ▼
进入客户服务器
        │
        ▼
堡垒机会话审计
        │
        ▼
会话结束
        │
        ▼
Webhook 推送日志
        │
        ▼
Agent 接收解析
        │
        ▼
生成工单时间线
```

优点

-   无需改造客户环境；
-   数据完整、统一；
-   与企业现有运维体系兼容；
-   实施成本低。

局限

-   必须通过堡垒机访问客户环境；
-   绕过堡垒机的操作无法采集。

------

#### SSH 终端录制

功能说明

针对未部署堡垒机的客户环境，可通过 Linux `script` 或 `asciinema` 工具录制终端会话。

登录时自动启动录制：

```bash
script /var/log/session.log
```

退出终端后自动结束录制，并上传日志。

流程

```text
SSH登录
    │
    ▼
启动script录制
    │
    ▼
记录所有输入输出
    │
    ▼
退出SSH
    │
    ▼
上传日志
    │
    ▼
Agent解析
```

优点

-   实现简单；
-   保留完整命令行操作；
-   无需额外平台支持。

局限

-   需要初始化客户服务器；
-   无法记录图形界面操作。

------

#### WebHook 数据接入

系统提供统一 WebHook 接口，允许第三方平台主动推送数据。

支持：

-   堡垒机；
-   运维平台；
-   自动化脚本；
-   企业监控平台；
-   CI/CD 平台。

统一接口：

```http
POST /api/v1/collector/webhook
```

支持：

-   JSON
-   Text
-   Multipart File

Webhook 接收后统一进入数据清洗流程。

------

#### 手工日志补录

考虑到特殊场景（如客户安全限制、无法自动采集），系统提供日志粘贴功能。

工程师可直接：

-   粘贴终端日志；
-   粘贴系统日志；
-   上传日志文件；
-   添加补充说明。

Agent 同样可进行解析。

此方案作为自动采集失败时的兜底机制。

------

#### 远程桌面录像关联

对于向日葵、ToDesk 等远程桌面工具，系统暂不进行视频内容解析，而是将录像作为工单附件进行关联。

支持：

-   上传录像文件；
-   保存录像路径；
-   与工单绑定；
-   后续人工查看。

二期可结合多模态模型实现视频内容分析。

------

### 数据标准化设计

由于不同来源的数据格式差异较大，需要统一转换为标准结构。

标准数据模型如下：

| 字段       | 类型     | 说明     |
| ---------- | -------- | -------- |
| source     | String   | 数据来源 |
| ticketId   | String   | 工单编号 |
| operator   | String   | 操作人   |
| timestamp  | DateTime | 操作时间 |
| operation  | String   | 操作描述 |
| command    | Text     | 执行命令 |
| output     | Text     | 命令输出 |
| level      | String   | 日志级别 |
| attachment | String   | 附件路径 |

示例：

```json
{
  "source": "JumpServer",
  "ticketId": "TK20260101001",
  "operator": "zhangsan",
  "timestamp": "2026-01-01 15:20:10",
  "command": "systemctl restart nginx",
  "output": "service started",
  "level": "INFO"
}
```

统一的数据结构便于 Agent 后续处理和存储。

------

### 数据处理流程

采集的数据需经过标准化处理后才能进入 Agent。

处理流程如下：

```text
采集原始数据
      │
      ▼
数据格式识别
      │
      ▼
数据清洗
（去除噪声、乱码）
      │
      ▼
命令解析
      │
      ▼
日志切片
      │
      ▼
结构化转换
      │
      ▼
保存数据库
      │
      ▼
Agent分析
```

主要处理内容包括：

-   字符编码统一（UTF-8）；
-   时间格式统一；
-   敏感信息脱敏（IP、密码、Token 等）；
-   日志切片；
-   命令识别；
-   错误信息提取。

------

### 数据安全设计

采集的数据涉及客户环境信息和运维操作记录，需重点保障数据安全。

#### 数据脱敏

自动识别并脱敏以下信息：

-   用户密码；
-   Access Token；
-   SSH 私钥；
-   数据库连接串；
-   身份证号；
-   手机号（可选）。

例如：

```text
mysql -uroot -p******
```

避免敏感信息进入工单和知识库。

------

#### 数据加密

日志文件及附件可采用服务器文件系统权限控制，重要数据可结合数据库字段加密或对象存储加密机制，保障静态数据安全。

------

#### 权限控制

仅授权用户可查看对应工单的日志和附件。

权限控制遵循 RBAC 模型：

-   工程师：查看本人负责工单；
-   管理员：查看全部工单；
-   审计人员：查看日志，不可修改。

------

### 适用场景分析

#### 适用场景

本方案适用于以下业务类型：

| 场景                 | 是否适用 | 说明            |
| -------------------- | -------- | --------------- |
| 教育信息化私有化部署 | √        | SSH 运维场景    |
| 企业 ERP 私有化      | √        | 堡垒机运维      |
| 医疗 HIS 系统        | √        | VPN + SSH       |
| 本地化一体机         | √        | SSH 或 Web 管理 |
| 单租户云部署         | √        | 跳板机运维      |
| 边缘节点设备         | √        | SSH 运维        |

------

#### 不适用场景

以下场景不适合作为主要采集方式：

| 场景             | 原因               |
| ---------------- | ------------------ |
| 多租户 SaaS      | 无客户独立运维过程 |
| 电话指导排障     | 无实际操作记录     |
| 禁止日志采集环境 | 无法获取运维数据   |

对于上述场景，可采用人工录入或上传日志文件方式作为补充。

------

### 数据采集与 Agent 协同流程

采集模块与 Agent 的协同关系如下：

```text
客户报障
    │
    ▼
创建工单
    │
    ▼
工程师远程处理
    │
    ▼
数据采集模块
    │
    ├── 堡垒机会话
    ├── SSH日志
    ├── 系统日志
    ├── 工程师备注
    └── 工单附件
          │
          ▼
      数据标准化
          │
          ▼
     PostgreSQL存储
          │
          ▼
    Agent智能分析
          │
          ▼
   自动生成标准工单
          │
          ▼
   知识提取与向量化
```

------

### 本章小结

本章设计了系统的数据采集体系，提出了以**堡垒机会话采集为主、SSH 终端录制为辅、WebHook 接入和人工补录兜底**的多源数据采集方案，并设计了统一的数据标准化模型和处理流程。

通过充分利用企业现有运维基础设施，在不改变工程师工作习惯的前提下，实现售后过程数据的自动采集、统一存储和安全管理，为 Agent 智能分析、工单自动生成和知识沉淀提供高质量的数据基础，同时兼顾实施成本、可扩展性和安全性，为系统后续智能化能力建设奠定了坚实基础。



------

## 数据模型设计

### 概述

数据模型是轻量化售后工单系统的核心基础，承担业务数据存储、流程管理、日志审计、AI 分析及知识沉淀等功能。

本系统采用 **PostgreSQL** 作为关系型数据库，存储业务数据；采用 **Chroma** 存储向量数据；Redis 用于缓存及异步任务。

数据库设计遵循以下原则：

-   **规范化设计**：满足第三范式（3NF），减少数据冗余。
-   **适度冗余**：针对查询频繁的字段进行冗余，提高查询效率。
-   **可扩展设计**：预留 AI、知识库、向量索引等字段。
-   **统一审计**：所有核心业务表均保留创建、修改及逻辑删除字段。
-   **JSON 扩展**：充分利用 PostgreSQL JSONB 存储灵活结构数据。

------

### 数据库总体设计

系统数据库由 **11 个核心业务表**和 **4 个基础管理表**组成。

#### 核心业务表

| 表名                 | 说明             |
| -------------------- | ---------------- |
| ticket               | 工单主表         |
| ticket_timeline      | 工单时间线       |
| ticket_operation_log | 工单操作日志     |
| customer             | 客户信息         |
| customer_contact     | 客户联系人       |
| notification         | 消息通知         |
| report_task          | 报表导出任务     |
| ai_task              | Agent任务        |
| ai_summary           | AI生成内容       |
| knowledge            | 知识条目（二期） |
| knowledge_embedding  | 向量索引（二期） |

------

#### 基础管理表

| 表名           | 说明     |
| -------------- | -------- |
| sys_user       | 用户     |
| sys_role       | 角色     |
| sys_permission | 权限     |
| sys_config     | 系统配置 |

------

#### 数据关系图

```text
                    Customer
                        │1
                        │
                        │N
                    Ticket
      ┌─────────────┼─────────────┐
      │             │             │
      ▼             ▼             ▼
 Timeline      OperationLog     AI Summary
      │
      ▼
 Knowledge（二期）

User
 │
 │1
 │
 │N
Ticket

Role
 │
 │N
 │
User
```

------

### 公共字段设计

所有业务表统一包含以下公共字段。

| 字段        | 类型      | 说明     |
| ----------- | --------- | -------- |
| id          | BIGSERIAL | 主键     |
| create_time | TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | 更新时间 |
| create_by   | BIGINT    | 创建人   |
| update_by   | BIGINT    | 修改人   |
| deleted     | BOOLEAN   | 逻辑删除 |
| remark      | TEXT      | 备注     |

统一字段便于审计和后续扩展。

------

### 工单主表（ticket）

#### 表说明

工单表是系统核心业务表，记录售后工单完整信息。

------

字段设计

| 字段            | 类型         | 说明                |
| --------------- | ------------ | ------------------- |
| id              | BIGSERIAL    | 主键                |
| ticket_no       | VARCHAR(32)  | 工单编号            |
| title           | VARCHAR(200) | 工单标题            |
| customer_id     | BIGINT       | 客户ID              |
| contact_id      | BIGINT       | 联系人ID            |
| category_id     | BIGINT       | 问题分类            |
| priority        | SMALLINT     | 优先级              |
| status          | SMALLINT     | 工单状态            |
| source          | VARCHAR(30)  | 来源（电话/微信等） |
| description     | TEXT         | 原始问题描述        |
| fault_summary   | TEXT         | 故障现象            |
| troubleshooting | TEXT         | 排查过程            |
| solution        | TEXT         | 解决方案            |
| root_cause      | TEXT         | 根因分析            |
| ai_summary      | TEXT         | AI总结              |
| handler_id      | BIGINT       | 当前处理人          |
| start_time      | TIMESTAMP    | 开始处理时间        |
| finish_time     | TIMESTAMP    | 完成时间            |
| duration        | INTEGER      | 处理时长（分钟）    |
| knowledge_id    | BIGINT       | 知识条目ID（二期）  |
| extra           | JSONB        | 扩展字段            |

------

索引设计

| 索引            | 字段        |
| --------------- | ----------- |
| UK_ticket_no    | ticket_no   |
| IDX_status      | status      |
| IDX_customer    | customer_id |
| IDX_handler     | handler_id  |
| IDX_finish_time | finish_time |

------

### 工单时间线表（ticket_timeline）

表说明

记录工单全过程事件。

字段设计

| 字段         | 类型         | 说明       |
| ------------ | ------------ | ---------- |
| id           | BIGSERIAL    | 主键       |
| ticket_id    | BIGINT       | 工单ID     |
| node_time    | TIMESTAMP    | 时间       |
| node_type    | VARCHAR(50)  | 节点类型   |
| title        | VARCHAR(100) | 节点名称   |
| content      | TEXT         | 内容       |
| operator     | VARCHAR(50)  | 操作人     |
| ai_generated | BOOLEAN      | 是否AI生成 |

例如：

```text
创建工单

↓

开始处理

↓

SSH登录

↓

执行命令

↓

Agent总结

↓

完成
```

------

### 工单操作日志（ticket_operation_log）

记录所有操作行为。

| 字段           | 类型         |
| -------------- | ------------ |
| id             | BIGSERIAL    |
| ticket_id      | BIGINT       |
| operation      | VARCHAR(100) |
| operator_id    | BIGINT       |
| operation_time | TIMESTAMP    |
| before_data    | JSONB        |
| after_data     | JSONB        |
| ip             | VARCHAR(50)  |

支持完整审计。

------

### 客户信息表（customer）

字段设计

| 字段        | 类型         |
| ----------- | ------------ |
| id          | BIGSERIAL    |
| name        | VARCHAR(200) |
| region      | VARCHAR(100) |
| version     | VARCHAR(50)  |
| deploy_type | VARCHAR(50)  |
| os          | VARCHAR(50)  |
| db_type     | VARCHAR(50)  |
| description | TEXT         |

------

### 联系人表（customer_contact）

| 字段        | 类型         |
| ----------- | ------------ |
| id          | BIGSERIAL    |
| customer_id | BIGINT       |
| name        | VARCHAR(100) |
| phone       | VARCHAR(50)  |
| email       | VARCHAR(100) |
| wechat      | VARCHAR(100) |
| position    | VARCHAR(100) |
| is_primary  | BOOLEAN      |

一个客户可对应多个联系人。

------

### 用户表（sys_user）

字段设计

| 字段        | 类型         |
| ----------- | ------------ |
| id          | BIGSERIAL    |
| username    | VARCHAR(100) |
| real_name   | VARCHAR(100) |
| phone       | VARCHAR(50)  |
| email       | VARCHAR(100) |
| ding_userid | VARCHAR(100) |
| department  | VARCHAR(100) |
| role_id     | BIGINT       |
| status      | SMALLINT     |

------

### 角色表（sys_role）

| 字段        | 类型        |
| ----------- | ----------- |
| id          | BIGSERIAL   |
| role_name   | VARCHAR(50) |
| role_code   | VARCHAR(50) |
| description | TEXT        |

角色：

-   超级管理员
-   运维管理员
-   售后工程师
-   查看人员

------

### 权限表（sys_permission）

采用 RBAC。

字段：

| 字段       | 类型         |
| ---------- | ------------ |
| id         | BIGSERIAL    |
| parent_id  | BIGINT       |
| name       | VARCHAR(100) |
| type       | SMALLINT     |
| path       | VARCHAR(200) |
| permission | VARCHAR(100) |

支持：

-   菜单
-   按钮
-   API

------

### 消息通知表（notification）

| 字段        | 类型         |
| ----------- | ------------ |
| id          | BIGSERIAL    |
| receiver_id | BIGINT       |
| title       | VARCHAR(200) |
| content     | TEXT         |
| type        | SMALLINT     |
| status      | SMALLINT     |
| send_time   | TIMESTAMP    |
| read_time   | TIMESTAMP    |

支持：

-   系统通知
-   WebSocket
-   钉钉消息

------

### AI任务表（ai_task）

用于记录 Agent 调用。

| 字段        | 类型        |
| ----------- | ----------- |
| id          | BIGSERIAL   |
| ticket_id   | BIGINT      |
| task_type   | VARCHAR(50) |
| status      | SMALLINT    |
| prompt      | TEXT        |
| response    | TEXT        |
| start_time  | TIMESTAMP   |
| finish_time | TIMESTAMP   |
| duration    | INTEGER     |

支持：

-   重试
-   调试
-   性能统计

------

### AI生成内容表（ai_summary）

记录 Agent 输出内容。

| 字段       | 类型         |
| ---------- | ------------ |
| id         | BIGSERIAL    |
| ticket_id  | BIGINT       |
| summary    | TEXT         |
| fault      | TEXT         |
| solution   | TEXT         |
| root_cause | TEXT         |
| tags       | VARCHAR(500) |
| confidence | NUMERIC(5,2) |

便于人工确认。

------

### 知识库表（二期）

knowledge

| 字段      | 类型         |
| --------- | ------------ |
| id        | BIGSERIAL    |
| title     | VARCHAR(300) |
| category  | VARCHAR(100) |
| summary   | TEXT         |
| solution  | TEXT         |
| ticket_id | BIGINT       |
| tags      | VARCHAR(500) |

------

knowledge_embedding

| 字段            | 类型         |
| --------------- | ------------ |
| id              | BIGSERIAL    |
| knowledge_id    | BIGINT       |
| embedding_model | VARCHAR(100) |
| vector_id       | VARCHAR(100) |
| content_hash    | VARCHAR(64)  |
| created_time    | TIMESTAMP    |

实际向量数据存储于 Chroma，关系库仅保存关联信息和元数据，便于管理和重建索引。

------

### 报表任务表（report_task）

记录导出任务。

| 字段        | 类型         |
| ----------- | ------------ |
| id          | BIGSERIAL    |
| report_name | VARCHAR(200) |
| creator     | BIGINT       |
| status      | SMALLINT     |
| file_path   | VARCHAR(500) |
| create_time | TIMESTAMP    |

采用异步生成。

------

### 数据关系说明

系统主要实体关系如下：

```text
User
 │
 │1
 │
 │N
Ticket
 │
 ├───────────────┐
 │               │
 ▼               ▼
Timeline      OperationLog
 │
 ▼
AISummary
 │
 ▼
Knowledge（二期）

Customer
 │
 │1
 │
 │N
Contact

Customer
 │
 │1
 │
 │N
Ticket
```

各实体之间通过外键进行关联，确保数据完整性和一致性。

------

### 数据生命周期

系统数据按照工单生命周期进行管理：

```text
创建工单
    │
    ▼
处理中
    │
    ▼
AI生成总结
    │
    ▼
工程师确认
    │
    ▼
工单完成
    │
    ▼
知识提取
    │
    ▼
向量化存储
    │
    ▼
历史归档
```

其中：

-   **业务数据**长期保存在 PostgreSQL；
-   **缓存数据**存储于 Redis，按策略过期；
-   **向量数据**存储于 Chroma；
-   **日志与附件**存储于文件系统或对象存储，并与工单建立关联。

------

### 数据库设计规范

为保证系统可维护性，数据库设计遵循以下规范：

1.  **命名规范**
    -   表名、字段名采用小写下划线命名（snake_case）。
    -   主键统一使用 `id`。
    -   外键统一采用 `<表名>_id` 格式。
2.  **字段规范**
    -   时间统一使用 `TIMESTAMP`。
    -   金额、评分等精度字段使用 `NUMERIC`。
    -   可扩展数据使用 `JSONB`。
3.  **索引规范**
    -   所有业务唯一编号建立唯一索引。
    -   高频查询字段建立普通索引。
    -   避免冗余索引和大字段索引。
4.  **删除规范**
    -   核心业务数据采用逻辑删除。
    -   日志、审计数据原则上不允许删除。

------

### 本章小结

本章完成了系统数据模型设计，明确了工单、客户、用户、消息、AI 任务、知识库等核心实体及其关系，构建了完整的数据存储体系。数据库采用 **PostgreSQL + Chroma + Redis** 的组合架构，实现了结构化业务数据、缓存数据及向量数据的分层存储，并预留了知识库及 AI 扩展字段。

该数据模型既满足一期售后工单管理和 Agent 智能分析需求，又为二期知识库、RAG 检索、多 Agent 协同等功能提供了良好的扩展能力。



------

## 接口设计

### 概述

系统采用 **RESTful API** 作为服务间及前后端通信的统一接口规范，前端（Vue3）、FastAPI 主业务服务、Flask Agent 服务均通过 HTTP/HTTPS 进行数据交互。

接口设计遵循以下原则：

-   **RESTful 风格**：资源导向，统一 URI 命名规范。
-   **统一认证**：采用 JWT Token 鉴权，支持钉钉 OAuth2 登录。
-   **统一响应格式**：所有接口返回统一 JSON 数据结构。
-   **统一异常处理**：定义标准错误码和错误信息。
-   **接口版本化**：统一采用 `/api/v1/` 作为接口前缀，便于后续版本升级。
-   **前后端分离**：所有业务操作均通过 API 完成，前端不直接访问数据库。

------

### 接口架构设计

系统接口主要分为三类：

1.  前端调用 FastAPI 业务接口；
2.  FastAPI 调用 Agent 服务接口；
3.  FastAPI 调用第三方系统接口（钉钉、消息中台等）。

整体架构如下：

```text
                 Vue3 前端
                     │
             HTTP / HTTPS
                     │
        ┌────────────▼────────────┐
        │     FastAPI REST API     │
        └───────┬─────────┬────────┘
                │         │
                │         │
          Agent API   第三方接口
                │         │
         Flask Agent   钉钉/消息中台
```

------

### 接口规范

#### URL 规范

统一格式：

```text
/api/v1/{module}/{resource}
```

例如：

```text
/api/v1/tickets
/api/v1/customers
/api/v1/users
```

------

#### HTTP 方法

| 方法   | 用途             |
| ------ | ---------------- |
| GET    | 查询数据         |
| POST   | 创建数据         |
| PUT    | 更新数据         |
| DELETE | 删除数据         |
| PATCH  | 部分更新（预留） |

------

#### 请求头

统一请求头：

```http
Authorization: Bearer {JWT Token}
Content-Type: application/json
```

文件上传：

```http
Content-Type: multipart/form-data
```

------

#### 统一响应格式

所有接口返回统一 JSON：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

分页数据：

```json
{
  "code":200,
  "message":"success",
  "data":{
      "total":120,
      "page":1,
      "pageSize":20,
      "records":[]
  }
}
```

异常：

```json
{
  "code":40001,
  "message":"Ticket Not Found"
}
```

------

### 登录认证接口

#### 钉钉登录

获取登录地址

```http
GET /api/v1/auth/login
```

返回：

```json
{
    "url":"https://..."
}
```

------

登录回调

```http
POST /api/v1/auth/callback
```

返回：

```json
{
    "token":"JWT",
    "expire":7200
}
```

------

获取当前用户

```http
GET /api/v1/auth/me
```

返回：

```json
{
  "id":1,
  "name":"张三",
  "role":"Engineer"
}
```

------

### 工作台接口

我的待办

```http
GET /api/v1/dashboard/todo
```

参数：

| 参数     | 类型    |
| -------- | ------- |
| page     | Integer |
| pageSize | Integer |

返回：

待处理工单列表。

------

我的进行中

```http
GET /api/v1/dashboard/processing
```

------

本周完成

```http
GET /api/v1/dashboard/completed
```

------

快速创建工单

```http
POST /api/v1/dashboard/quick-ticket
```

------

### 工单中心接口

创建工单

```http
POST /api/v1/tickets
```

请求：

```json
{
  "title":"登录异常",
  "customerId":1,
  "priority":2,
  "description":"系统无法登录"
}
```

------

查询工单列表

```http
GET /api/v1/tickets
```

支持：

-   状态
-   客户
-   时间
-   工程师
-   分页

------

查询工单详情

```http
GET /api/v1/tickets/{id}
```

返回：

-   基本信息
-   时间线
-   AI总结
-   附件

------

更新工单

```http
PUT /api/v1/tickets/{id}
```

------

删除工单

```http
DELETE /api/v1/tickets/{id}
```

采用逻辑删除。

------

指派工单

```http
POST /api/v1/tickets/{id}/assign
```

请求：

```json
{
    "userId":12
}
```

------

更新工单状态

```http
POST /api/v1/tickets/{id}/status
```

请求：

```json
{
    "status":"PROCESSING"
}
```

------

### 客户管理接口

客户列表

```http
GET /api/v1/customers
```

------

新增客户

```http
POST /api/v1/customers
```

------

修改客户

```http
PUT /api/v1/customers/{id}
```

------

删除客户

```http
DELETE /api/v1/customers/{id}
```

------

联系人管理

```http
GET /api/v1/customers/{id}/contacts

POST /api/v1/customers/{id}/contacts

PUT /api/v1/contacts/{id}

DELETE /api/v1/contacts/{id}
```

------

### 报表中心接口

工单统计

```http
GET /api/v1/reports/ticket-summary
```

返回：

-   总工单
-   已完成
-   待处理
-   超时

------

工作量统计

```http
GET /api/v1/reports/workload
```

------

Excel导出

```http
POST /api/v1/reports/export
```

异步返回：

```json
{
  "taskId":123
}
```

查询：

```http
GET /api/v1/reports/export/{taskId}
```

------

### 消息中心接口

获取通知

```http
GET /api/v1/notifications
```

------

已读通知

```http
PUT /api/v1/notifications/{id}/read
```

------

WebSocket

连接：

```text
/ws/notification
```

消息：

```json
{
  "type":"ticket",
  "title":"工单已指派"
}
```

------

### 系统管理接口

用户

```http
GET /api/v1/users

POST /api/v1/users

PUT /api/v1/users/{id}

DELETE /api/v1/users/{id}
```

------

角色

```http
GET /api/v1/roles

POST /api/v1/roles
```

------

权限

```http
GET /api/v1/permissions
```

------

系统配置

```http
GET /api/v1/config

PUT /api/v1/config
```

------

### Agent 接口设计

FastAPI 不直接调用 LLM，而是调用 Agent 微服务。

------

日志解析

```http
POST /agent/log/parse
```

请求：

```json
{
    "ticketId":1001,
    "log":"..."
}
```

返回：

```json
{
    "summary":"..."
}
```

------

工单生成

```http
POST /agent/ticket/generate
```

请求：

```json
{
    "ticketId":1001
}
```

返回：

```json
{
    "fault":"...",
    "solution":"...",
    "rootCause":"..."
}
```

------

故障分析

```http
POST /agent/fault/analyze
```

------

知识提取

```http
POST /agent/knowledge/extract
```

------

相似案例推荐

```http
POST /agent/search
```

请求：

```json
{
    "question":"Redis启动失败"
}
```

返回：

推荐工单。

------

### 数据采集接口

WebHook

```http
POST /api/v1/collector/webhook
```

支持：

-   JSON
-   TXT
-   Multipart

------

上传日志

```http
POST /api/v1/collector/upload
```

上传：

-   txt
-   log
-   zip

------

手工日志

```http
POST /api/v1/collector/manual
```

请求：

```json
{
    "ticketId":1001,
    "content":"..."
}
```

------

### 第三方接口

一期规划对接以下第三方平台：

| 平台         | 接口用途          |
| ------------ | ----------------- |
| 钉钉 OAuth2  | 用户登录认证      |
| 钉钉机器人   | 工单通知          |
| JumpServer   | 获取会话日志      |
| Teleport     | 获取 SSH 审计日志 |
| 消息中台     | 消息推送          |
| LDAP（预留） | 用户同步          |

------

### 错误码设计

统一错误码。

| 错误码 | 说明          |
| ------ | ------------- |
| 200    | 成功          |
| 40000  | 参数错误      |
| 40001  | 数据不存在    |
| 40002  | 状态非法      |
| 40100  | 未登录        |
| 40101  | Token失效     |
| 40300  | 无权限        |
| 40400  | 接口不存在    |
| 50000  | 系统异常      |
| 50001  | 数据库异常    |
| 50002  | Agent调用失败 |
| 50003  | LLM超时       |

------

### 接口安全设计

系统所有接口均需通过统一认证与权限控制，保障数据安全和访问可控。

#### 身份认证

-   用户登录采用钉钉 OAuth2 获取身份信息。
-   登录成功后由系统签发 JWT Token。
-   后续请求通过 `Authorization: Bearer <Token>` 进行身份认证。
-   Token 设置有效期，并支持刷新机制。

------

#### 权限控制

所有业务接口均结合 RBAC 模型进行权限校验，支持：

-   菜单权限；
-   功能按钮权限；
-   API 权限；
-   数据权限（仅查看本人或所属部门工单等）。

权限校验通过 FastAPI 的 `Depends` 机制统一实现。

------

#### 接口限流

为防止恶意调用和资源滥用，建议对关键接口增加限流策略：

| 接口       | 建议策略                       |
| ---------- | ------------------------------ |
| 登录接口   | 同一 IP 每分钟限制 10 次       |
| Agent 接口 | 同一用户并发任务数限制         |
| 文件上传   | 限制文件大小与上传频率         |
| 导出接口   | 同一用户同时仅允许一个导出任务 |

可结合 Redis 实现令牌桶或滑动窗口限流。

------

#### 数据校验

所有接口请求均进行统一参数校验：

-   必填字段校验；
-   数据类型校验；
-   字段长度校验；
-   枚举值校验；
-   SQL 注入及 XSS 防护。

FastAPI 使用 Pydantic 模型完成请求参数验证。

------

### 接口调用流程

系统各模块之间的典型调用流程如下：

```text
Vue3 前端
     │
     ▼
FastAPI REST API
     │
     ├──────────────┐
     ▼              ▼
PostgreSQL      Flask Agent
                     │
                     ▼
                 LLM 模型
                     │
                     ▼
              返回结构化结果
                     │
                     ▼
              FastAPI 保存数据
                     │
                     ▼
                 前端展示结果
```

对于耗时任务（如 Agent 分析、报表导出），FastAPI 将任务提交至 Celery，由 Worker 异步执行，完成后通过 WebSocket 或消息通知告知前端。

------

### 本章小结

本章完成了系统接口设计，定义了前后端通信规范、RESTful API 风格、统一请求与响应格式，以及认证、权限、异常处理等基础规范。

围绕工作台、工单中心、客户管理、报表中心、消息中心、系统管理、数据采集及 Agent 智能服务等模块，设计了完整的接口体系，并明确了第三方平台集成方式和接口安全策略。通过 FastAPI 统一承载业务 API、Flask Agent 提供 AI 能力，实现了业务逻辑与智能分析服务的解耦，为系统开发、联调及后续版本扩展提供了统一、规范的接口基础。



我建议将**第八章重新定义为"部署设计"**。因为前面已经单独写了一章 **Agent 智能体设计**，如果第八章仍然写 Agent，就会与整体文档结构重复，不符合标准 SDD（Software Design Document）的章节组织。

按照完整的软件设计文档，推荐采用下面的章节顺序：

>   第1章 项目概述
>   第2章 总体架构设计
>   第3章 功能设计
>   第4章 Agent 智能体设计
>   第5章 数据采集设计
>   第6章 数据模型设计
>   第7章 接口设计
>   **第8章 部署设计**
>   第9章 安全设计
>   第10章 二期规划

下面是重新整理后的**第八章**。

------

## 部署设计

###  概述

为满足轻量化、快速部署、易于维护和可扩展的建设目标，系统采用 **Docker Compose** 作为一期部署方案，通过容器化方式统一管理前端、业务服务、Agent 服务、数据库、缓存及向量数据库等组件，实现一键部署、快速升级和环境一致性。

系统采用**前后端分离 + 微服务**部署模式，业务服务与 AI 服务独立运行，既保证业务稳定性，又便于后续横向扩容和模型升级。

部署设计遵循以下原则：

-   **容器化部署**：所有组件均运行于 Docker 容器中，降低环境依赖。
-   **服务解耦**：FastAPI 与 Agent 独立部署，可按需扩容。
-   **配置集中管理**：环境变量统一管理，便于不同环境切换。
-   **数据持久化**：数据库、向量库、日志及上传文件均采用挂载卷保存。
-   **易维护、易升级**：支持版本回滚、服务热更新及日志统一管理。

------

### 系统部署架构

系统整体部署架构如下：

```text
                 Internet / 内网
                       │
                  Nginx 反向代理
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
    Vue3 前端                    FastAPI 服务
                                      │
                ┌─────────────────────┼────────────────────┐
                ▼                     ▼                    ▼
             PostgreSQL            Redis             Flask Agent
                                                           │
                                                           ▼
                                                     LLM 服务
                                                           │
                                                           ▼
                                                    Chroma 向量库
```

各组件职责如下：

| 组件        | 功能                       |
| ----------- | -------------------------- |
| Nginx       | 反向代理、HTTPS、静态资源  |
| Vue3        | 前端页面                   |
| FastAPI     | 工单业务、权限、报表、消息 |
| PostgreSQL  | 业务数据存储               |
| Redis       | 缓存、Celery Broker        |
| Flask Agent | AI 智能分析                |
| LLM         | 大语言模型                 |
| Chroma      | 向量数据库                 |

------

### Docker Compose 部署

一期采用 Docker Compose 编排所有服务。

部署组件如下：

| 服务        | 容器名称 |
| ----------- | -------- |
| nginx       | nginx    |
| 前端        | frontend |
| FastAPI     | fastapi  |
| Flask Agent | agent    |
| PostgreSQL  | postgres |
| Redis       | redis    |
| Chroma      | chroma   |

部署关系如下：

```text
docker-compose

│

├── nginx

├── frontend

├── fastapi

├── postgres

├── redis

├── chroma

└── flask-agent
```

启动命令：

```bash
docker compose up -d
```

停止：

```bash
docker compose down
```

------

### 服务部署说明

#### Nginx

负责：

-   HTTPS
-   API 转发
-   静态资源
-   Gzip 压缩
-   WebSocket 转发

主要代理规则：

```text
/

↓

Vue

/api

↓

FastAPI

/agent（内部）

↓

Flask
```

------

#### Vue3 前端

部署方式：

```text
npm build

↓

dist/

↓

Nginx
```

无需 Node 环境运行。

------

#### FastAPI

负责：

-   工单业务
-   登录认证
-   权限
-   报表
-   文件上传
-   消息推送

采用：

```
Uvicorn + Gunicorn
```

支持多 Worker。

------

#### Flask Agent

独立部署。

负责：

-   Prompt
-   Tool Calling
-   LangChain
-   JSON Parser
-   RAG

FastAPI 调用：

```
HTTP REST API
```

通信。

------

#### PostgreSQL

保存：

-   工单
-   用户
-   客户
-   日志
-   配置
-   AI结果

数据目录：

```text
/data/postgres
```

持久化保存。

------

#### Redis

负责：

-   Token
-   Cache
-   Celery
-   Session

目录：

```text
/data/redis
```

------

#### Chroma

保存：

-   Embedding
-   Knowledge

目录：

```text
/data/chroma
```

二期开启。

------

### 目录结构设计

建议部署目录：

```text
after-sale-system/

├── docker-compose.yml

├── nginx/

│ └── nginx.conf

├── frontend/

├── fastapi/

├── agent/

├── data/

│ ├── postgres/

│ ├── redis/

│ ├── chroma/

│ ├── uploads/

│ └── backup/

├── logs/

│ ├── fastapi/

│ ├── agent/

│ └── nginx/

└── scripts/
```

------

### 环境配置

统一采用 `.env` 管理环境变量。

示例：

| 变量            | 说明            |
| --------------- | --------------- |
| DB_HOST         | PostgreSQL 地址 |
| DB_PORT         | PostgreSQL 端口 |
| DB_NAME         | 数据库名称      |
| DB_USER         | 用户名          |
| DB_PASSWORD     | 密码            |
| REDIS_HOST      | Redis 地址      |
| AGENT_URL       | Agent 服务地址  |
| CHROMA_URL      | Chroma 地址     |
| JWT_SECRET      | JWT 密钥        |
| DING_APP_KEY    | 钉钉 AppKey     |
| DING_APP_SECRET | 钉钉 Secret     |

------

### 日志管理

日志按服务分类保存。

目录：

```text
logs/

├── fastapi/

├── agent/

├── nginx/

├── celery/

└── scheduler/
```

日志内容包括：

-   API 调用日志
-   SQL 日志
-   Agent 调用日志
-   Prompt 日志（可选）
-   异常日志

建议按天切分，并保留最近 30 天。

------

### 数据备份

系统支持自动备份：

| 数据       | 方式         |
| ---------- | ------------ |
| PostgreSQL | pg_dump      |
| 上传附件   | 文件复制     |
| Chroma     | 数据目录备份 |
| 配置文件   | Git 管理     |

备份流程：

```text
定时任务

↓

pg_dump

↓

backup/

↓

压缩

↓

上传NAS/OSS（可选）
```

建议每日凌晨执行全量备份，并保留不少于 7 个历史版本。

------

### 系统升级

升级流程：

```text
停止服务

↓

备份数据库

↓

拉取新镜像

↓

docker compose pull

↓

docker compose up

↓

数据库迁移

↓

完成
```

数据库迁移建议使用 **Alembic** 管理版本。

------

### 系统监控

建议监控内容：

| 指标           | 工具               |
| -------------- | ------------------ |
| CPU            | Docker             |
| 内存           | Docker             |
| PostgreSQL     | pg_stat            |
| Redis          | INFO               |
| Agent 调用耗时 | 自定义日志         |
| Celery 队列    | Flower（可选）     |
| 接口响应时间   | FastAPI Middleware |

后续可接入 Prometheus + Grafana 实现统一监控。

------

### 部署流程

系统首次部署流程如下：

```text
安装 Docker

↓

安装 Docker Compose

↓

配置 .env

↓

启动 PostgreSQL

↓

启动 Redis

↓

启动 Chroma

↓

启动 FastAPI

↓

启动 Flask Agent

↓

启动 Vue

↓

启动 Nginx

↓

系统运行
```

------

### 高可用扩展（后续）

随着业务规模扩大，可逐步升级部署架构：

-   Nginx 配置负载均衡；
-   FastAPI 多实例部署；
-   Agent 服务按任务量横向扩容；
-   PostgreSQL 主从复制；
-   Redis 哨兵模式；
-   Chroma 集群（或迁移至 Milvus）；
-   Kubernetes 容器编排，实现弹性扩缩容和滚动发布。

------

### 本章小结

本章完成了系统部署设计，采用 **Docker Compose + Nginx + FastAPI + Flask Agent + PostgreSQL + Redis + Chroma** 的轻量化部署架构，实现了前后端分离、业务与 AI 服务解耦、数据持久化及统一配置管理。

一期方案兼顾快速落地和低运维成本，支持一键部署、自动备份、日志管理和版本升级；同时预留了 Kubernetes、高可用数据库、分布式缓存和监控平台等扩展能力，为系统后续规模化部署和持续演进提供了良好的基础。







