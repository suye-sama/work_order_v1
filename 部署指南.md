# 部署指南

> 从 Windows 本地开发环境迁移到 Linux 生产服务器

---

## 部署平台选择：推荐 Linux 服务器

### Windows vs Linux 对比

| 维度 | Windows Server | Linux (CentOS/Ubuntu) |
|------|---------------|----------------------|
| **FastAPI** | ✅ 可运行 | ✅ 原生支持，性能更好 |
| **Flask Agent** | ✅ 可运行 | ✅ 原生支持 |
| **Vue3 前端** | ✅ Nginx/IIS | ✅ Nginx，生态成熟 |
| **PostgreSQL** | ⚠️ Windows 版有性能损耗 | ✅ 原生支持 |
| **Redis** | ⚠️ 微软维护的旧版，已停更 | ✅ 原生支持 |
| **Celery 异步** | ❌ Windows 需 `--pool=solo`，不支持并发 | ✅ 原生支持多 Worker |
| **ChromaDB** | ✅ 可运行 | ✅ 原生支持 |
| **Docker** | ⚠️ Docker Desktop 内存占用大 | ✅ 原生 Docker，资源效率高 |
| **运维工具** | 较少 | systemd 服务管理、Nginx 反向代理、日志轮转 |
| **社区资源** | 少 | 丰富（教程/踩坑/最佳实践） |
| **成本** | Windows Server 需付费授权 | 免费开源 |

### 结论

**必须部署到 Linux 上**。主要原因：

1. **Celery 致命缺陷**：Windows 下 Celery 只能单进程运行（`--pool=solo`），无法并发处理 Agent 任务。Agent 调用 LLM 需 10-20 秒，多用户同时使用时 Windows 会严重阻塞。

2. **生产中间件不支持 Windows**：Redis 官方已停止维护 Windows 版，PostgreSQL 在 Windows 上性能有明显损耗。

3. **Docker 不可用**：开发文档设计的 Docker Compose 一键部署方案依赖 Linux Docker 环境。Windows Docker Desktop 仅适合开发，不适合生产。

---

## 部署前准备清单

### 一、服务器准备

最低配置：2 核 4G + 40G SSD（约 50-80 元/月）
推荐配置：4 核 8G + 60G SSD（约 100-150 元/月）

可选云服务商：阿里云 ECS / 腾讯云 CVM / 华为云 ECS

### 二、环境安装（CentOS/Ubuntu）

```bash
# 1. 基础工具
sudo apt update && sudo apt install -y git curl nginx python3.11 python3-pip nodejs npm

# 2. Docker + Docker Compose（用于 PostgreSQL/Redis/Chroma）
curl -fsSL https://get.docker.com | bash
sudo apt install -y docker-compose

# 3. 拉取项目代码
git clone <your-repo> /opt/work_order
cd /opt/work_order
```

### 三、需要改造的代码

| 改造项 | 当前状态（Windows 学习） | 部署前需改为 |
|--------|------------------------|-------------|
| **数据库** | SQLite（`data/work_order.db`） | PostgreSQL（Docker 容器） |
| **Celery** | 未启用 | 启用异步任务队列（Agent 调用 + Excel 导出） |
| **前端 Build** | `npm run dev` 开发模式 | `npm run build` 生产模式 → Nginx 静态托管 |
| **静态文件** | Vite 开发代理 | Nginx 统一代理 `/` → 前端，`/api` → FastAPI |
| **环境变量** | `.env` 明文 | 服务器环境变量或 `.env` 文件，API Key 妥善保管 |
| **CORS** | `allow_origins=["*"]` | 限制为实际域名 |
| **调试模式** | Flask `debug=True`、FastAPI `--reload` | 关闭所有调试开关 |
| **日志** | 控制台打印 | 写入文件 + 日志轮转 |

### 四、改造清单详解

#### 1. 数据库切 PostgreSQL

```python
# backend/app/config.py
# 改前
DATABASE_URL = "sqlite:///data/work_order.db"

# 改后
DATABASE_URL = "postgresql://work_order:password@127.0.0.1:5432/work_order_v1"
```

#### 2. 前端变为静态部署

```bash
cd frontend
npm run build          # 生成 dist/ 静态文件
# 由 Nginx 直接托管，不再需要 Node.js 进程
```

#### 3. Nginx 统一入口

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /opt/work_order/frontend/dist;
        try_files $uri /index.html;
    }

    # API 代理到 FastAPI
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 4. systemd 服务管理

```ini
# /etc/systemd/system/workorder-api.service
[Unit]
Description=Work Order FastAPI
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/work_order/backend
ExecStart=/usr/bin/python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

```ini
# /etc/systemd/system/workorder-agent.service
[Unit]
Description=Work Order Flask Agent
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/work_order/agent
ExecStart=/usr/bin/python3 -m flask --app app.main run --host 127.0.0.1 --port 5000
Restart=always

[Install]
WantedBy=multi-user.target
```

### 五、部署后最终架构

```
外网用户
    │
    ▼
Nginx (80/443)
    │
    ├── /           → Vue3 静态文件 (dist/)
    ├── /api/*      → FastAPI :8000 (2-4 workers)
    └── /ws/*       → FastAPI WebSocket
                          │
         ┌────────────────┼────────────────┐
         ▼                ▼                ▼
    PostgreSQL        Redis          Flask Agent :5000
    (Docker)        (Docker)             │
                                    ┌────┴────┐
                                    ▼         ▼
                                  LLM      ChromaDB
                              (百炼API)   (Docker)
                          │
                    Celery Worker
                  (异步执行 Agent 任务)
```

---

## 推荐部署步骤

```
1. 购买一台 Linux 云服务器（2核4G 起步）
2. 安装 Docker + Python + Nginx
3. 拉取代码，配置 .env（LLM Key + 数据库密码）
4. docker compose up -d（启动 PG/Redis/Chroma）
5. 修改 config.py 切换 PostgreSQL
6. npm run build 编译前端
7. 配置 Nginx 反向代理
8. 配置 systemd 服务（FastAPI + Agent + Celery Worker）
9. 配置域名 + HTTPS（Let's Encrypt 免费证书）
10. 开放安全组端口（仅 80/443）
```
