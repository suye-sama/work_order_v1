# 原始文件

**==轻量化售后工单系统==**

## 项目背景

### 原有售后业务流程

客戶通过电话、微信、微信群等渠道反馈问题→工程师通过VPN/SSH/堡垒机/向日葵/ToDesk等工具
登录客戶环境→排查服务器及后台系统故障→完成问题修复处理→人工登录售后宝手动填写工单归
档。

### 现有业务痛点

原有售后模式完全依赖人工记录工单，存在诸多效率和资产沉淀问题，核心痛点如下：
• 工单记录繁琐易错：工程师需手动梳理故障现象、排查过程、执行操作、解决方案、根因分析等全
维度内容，重复性工作多，耗时费力，且极易出现信息遗漏、内容写错等问题。
• 工单质量不统一：不同工程师的文字总结能力、记录习惯差异较大，工单内容格式混乱、详略不
一，无标准化规范，不利于后续复盘核查。
• 人员配合度低：技术人员核心工作为故障排查与问题修复，文书类工单记录工作冗余，导致工程师
抵触填写工单，敷衍记录情况频发。
• 技术知识无法沉淀：工程师核心价值的排查过程、SSH操作记录、日志分析过程、排查思路、故障
恢复动作等关键技术信息，无法结构化留存，优质售后处理经验难以转化为企业可复用的知识资
产，新人学习成本高、问题重复踩坑率高。

## 项目核心目标

针对传统售后工单模式的核心痛点，打造轻量化、自动化、智能化的售后工单管理系统，摆脱人工手
动记录的局限，实现售后流程数字化、知识资产结构化沉淀，具体目标如下：

1. 自动记录售后过程：实时捕获工程师故障排查全流程的关键操作、操作数据、日志信息等核心内
容，全程无需人工手动录入。
2. 自动生成结构化工单：基于实时抓取的售后处理过程数据，自动规整、生成标准化、规范化的完整
工单，统一工单质量标准。
3. 智能总结故障信息：通过智能解析算法，自动提取故障现象、核心排查步骤、最终解决方案、故障
根因等关键信息，完成工单核心内容总结。
4. 自动沉淀知识资产：将零散的售后处理经验、故障解决方案、排查思路，自动转化为结构化、可检
  索、可复用的企业知识库，完成技术经验长效沉淀。

## 系统核心功能模块

### 工作台模块

面向工程师个人的轻量化操作首页，聚焦个人日常售后工作，提升操作效率，核心功能：

-   我的待办：集中展⽰当前待处理、待跟进的所有工单，清晰展⽰优先级、截止时间、客戶信息，便
    于工程师快速承接工作。
-   我的进行中：实时展⽰当前正在处理的工单，同步更新处理进度、剩余节点，方便工程师统筹推进
    工作。
-   本周已完成：统计展⽰本周已办结的所有工单，支持快速查看详情，便于个人工作复盘与对账。
-   快速创建工单：支持一键手动创建工单，适配特殊场景下的临时工单录入需求，简化创建流程。

### 工单中心模块

系统核心业务模块，实现工单全生命周期管理，覆盖查询、查看、流转、编辑全场景：

-   工单列表：区分全部工单、我的工单、我创建的工单三大维度，支持按工单状态、客戶名称、问题
    类型、处理时间等多条件筛选、检索，快速定位目标工单。
-   工单详情：展⽰工单完整信息，包含工单基本信息、全程状态流转记录、处理节点操作、处理过程
    时间线、指派人变更记录、操作日志等，实现工单全程可追溯。

### 客户信息模块

统一管理售后客戶资源，实现工单与客戶精准关联，支撑业务高效开展：

-   客戶/学校列表：集中展⽰所有合作客戶/学校信息，包含名称、所属地区、使用系统版本、现场部
    署环境等基础信息，便于工程师提前了解客戶场景。
-   联系人管理：维护客戶对接人信息，包含姓名、联系方式、对接权限等，保障售后沟通顺畅。
-   工单关联客戶：创建工单时可直接检索、绑定对应客戶信息，实现工单与客戶数据联动，完善工单
    维度信息。

### 报表中心模块

实现售后数据可视化统计与导出，支撑管理层数据分析、工作量考核、业务优化决策：

-   基础数据统计：自动统计工单状态分布、问题类型分布等核心数据，直观展⽰售后业务整体情况。
-   工作量汇总：按日、周、月多周期维度，自动统计各工程师工单处理数量、办结率、待办量等工作
    量数据。
-   数据导出：支持所有统计报表、工单数据一键Excel导出，用于数据存档、考核对账、业务复盘。

### 消息推送模块

实现工单动态实时触达，避免信息滞后导致的工单延误，保障流程高效流转：

-   状态变更推送：工单新建、指派、处理中、办结、驳回等状态变更时，自动触发消息推送至消息中
    台。
-   系统内通知：平台内部实时弹窗、消息提醒，确保工程师及时接收工单动态、待办提醒、变更通
    知。

### 系统管理模块

系统基础配置与权限管控模块，保障系统安全、稳定、标准化运行：

-   用戶管理：支持钉钉快捷登录，统一管理系统用戶，基于岗位配置不同操作角色与权限，实现分级
    管控。
-   问题类型配置：自定义维护售后问题分类（如系统故障、服务器异常、功能bug、操作咨询等），
    统一工单分类标准。
-   消息中台接口配置：对接消息推送接口，配置推送渠道、推送规则，保障消息通知正常触达。
-   数据备份：支持系统工单数据、客戶数据、配置数据定期自动备份，保障业务数据安全不丢失。

### 知识库模块（二期规划）

一期仅预留完整数据结构、接口字段与页面框架，不实现完整功能；二期核心实现：工单完成回执自
动沉淀、拆解、规整为标准化知识条目，形成企业专属售后知识库，支持检索、复用、新人培训，实
现售后经验长效沉淀与共享。

# 系统思路

针对这套“轻量化售后工单系统”，可以围绕**自动化采集、智能分析、结构化沉淀**三条主线来设计实现思路，技术栈聚焦于 Python（FastAPI + Flask）、LLM Agent，并保持整体架构的轻量和可落地性。

---

## 总体架构思路：前后端分离 + Agent 中台

```
[前端轻量控制台] 
   ↓ HTTP/WebSocket
[FastAPI 主业务后端] ←→ [Flask Agent 微服务] ←→ LLM
   ↓
[PostgreSQL + Redis + 向量数据库]
```

- **FastAPI（主后端）**：负责工单 CRUD、用户认证、权限、报表、消息推送等核心业务逻辑，异步性能好，适合 IO 密集。
- **Flask（Agent 微服务）**：专门承接 AI 任务（工单总结、根因分析、知识提取），由于 Agent 调用链路复杂、可能包含多个工具调用，用轻量的 Flask 单独部署，方便独立扩缩容与调试。
- **数据库**：PostgreSQL 存储结构化业务数据；Redis 缓存及异步任务队列；向量数据库（如 Milvus Lite / Chroma）用于知识库语义检索。
- **前端**：Vue3 + Element Plus，打包成静态资源，Nginx 反代，极致轻量。

---

## 核心模块实现思路

### 工作台 & 工单中心（FastAPI 实现）
- **工单生命周期状态机**：`新建 → 待处理 → 处理中 → 待确认 → 已完成`，严格控制流转。
- **多维度列表查询**：利用 SQLAlchemy 构建灵活筛选条件，通过 Query 参数实现，分页与排序。
- **工单详情时间线**：将状态变更、操作日志、Agent 生成的阶段性总结存入 `timeline` 字段（JSON 数组），前端渲染成时间轴。
- **快速创建工单**：提供简化表单，自动关联当前登录用户、客户信息，可手动填写或粘贴原始故障描述，随后由 Agent 自动补全。

### 客户信息模块
- 建立 `customers`、`contacts` 表，与工单多对一关联。
- 前端通过搜索接口下拉选择客户，录入工单时带出客户环境信息（系统版本、部署环境），减少工程师手工输入。

### 报表中心
- 使用 SQL 聚合查询生成统计数据，FastAPI 直接返回数据。
- Excel 导出采用 `openpyxl` 流式写入，支持大数量工单导出。
- 定时任务（Celery + Redis）预计算日报、周报，减少实时计算压力。

### 消息推送模块
- 工单状态变更时，FastAPI 通过信号（如 SQLAlchemy 事件）触发推送。
- 系统内通知：WebSocket 实时推送至前端，并在 `notifications` 表中记录。
- 外部通知：对接钉钉机器人 Webhook，发送工单指派、超时提醒等；预留“消息中台接口配置”，用策略模式适配不同渠道。

### 系统管理
- **钉钉登录**：采用 OAuth2 授权码模式，获取用户信息后与本地用户表关联，自动创建账号。
- **RBAC 权限**：角色（管理员、工程师、只读）控制菜单和 API 访问，使用 FastAPI Depends 注入权限校验。
- **数据备份**：编写管理命令（Flask-Script 或 Click），通过 `pg_dump` 定时导出，存储到本地或云存储。

---

## Agent 智能体实现思路（关键部分，必须包含）

Agent 是系统的“智慧大脑”，主要负责**售后过程自动记录、工单自动生成、故障智能总结、知识自动沉淀**这四大目标。设计如下：

### Agent 架构
在 Flask 微服务中，基于 **LangChain / LlamaIndex** 搭建 Agent，可调用工具（Tools）和记忆（Memory），并接入 LLM（GPT-4 或本地 ChatGLM）。

### 售后过程自动记录 Agent
- **数据源**：假设工程师操作通过堡垒机或终端录制工具（asciinema）产生 `script.log`，Agent 定时拉取或通过 Webhook 接收日志文件。
- **处理流程**：
  - 读取会话日志，调用 LLM 进行分段总结，提取关键操作（SSH 登录、执行命令、报错信息、文件修改等）。
  - 将原始日志与阶段性摘要存入工单草稿，生成时间线条目。
- **实现轻量版**：提供一个简易的**浏览器端操作记录器**（前端 JS 或桌面客户端），工程师粘贴命令或终端日志，Agent 后台解析。

### 工单自动生成 Agent
当工程师提交“售后处理完成”信号（或定时触发）时，Agent 执行：
1. **信息聚合**：从工单关联的日志、客户环境信息、工程师备注中拼接完整上下文。
2. **链式调用 Prompt**：
   - 第一步：提取故障现象、影响范围。
   - 第二步：梳理关键排查步骤与思路。
   - 第三步：总结最终解决方案与根因分析。
3. **结构化输出**：使用 LangChain 的 `PydanticOutputParser`，强制 LLM 输出符合工单模版的 JSON（包含现象、排查步骤、解决方案、根因、处理人、耗时时长等字段）。
4. **人工确认**：生成的工单置为“待确认”状态，推送给工程师，仅需校对、微调后提交，大幅降低录入成本。

### 故障智能总结与知识沉淀 Agent
- 工单“已完成”后，触发 Agent：
  - 提取工单中的 `<现象, 根因, 解决方案>` 三元组。
  - 将三元组和完整工单文本进行 Embedding，存入向量数据库（Chroma）。
  - 同时生成标准化知识条目（标题、分类、标签、解决步骤），写入 `knowledge_base` 表（二期完整展示）。
- **检索复用**：工程师创建工单或遇到新故障时，Agent 自动在向量库中搜索相似历史工单，将“推荐解决方案”推送至工单处理界面，实现知识辅助。

### Agent 的工具增强
让 Agent 具备调用外部工具的能力，例如：
- **查询客户环境信息**：通过 API 调用 FastAPI 接口获取客户系统版本、部署参数。
- **执行简单诊断命令**（受限环境）：通过预定义的脚本远程获取服务状态，Agent 分析状态输出。
- **搜索内部文档**：接入企业 Wiki 或文档库，辅助原因分析。

---

## 轻量化与快速落地的关键设计

1. **单仓库多服务部署**：使用 Docker Compose 编排 FastAPI、Flask Agent、PostgreSQL、Redis、Chroma，一键启动。
2. **低依赖的前端**：首版甚至可以仅用后台管理模板（如 Ant Design Pro Vue），减少开发量。
3. **工单模版可配置**：在系统管理中 JSON 编辑工单字段模版，Agent 按模版生成，适配不同业务。
4. **消息和任务异步化**：Agent 调用 LLM 耗时长，采用 Celery 异步任务，前端轮询或 WebSocket 获取结果，避免阻塞。
5. **知识库二期接口预留**：一期在工单表中增加 `knowledge_id`、`embedding` 字段，并实现 API 占位，二期直接激活 Agent 流程即可，数据无缝迁移。

---

## 技术栈总结

| 层次      | 技术                 | 作用                           |
| --------- | -------------------- | ------------------------------ |
| 后端框架  | FastAPI              | 主业务 API，异步高性能         |
| AI 微服务 | Flask + LangChain    | Agent 服务，LLM 调用、工具串联 |
| 数据库    | PostgreSQL           | 业务数据持久化                 |
| 向量库    | Chroma / Milvus Lite | 知识库语义检索                 |
| 缓存/队列 | Redis + Celery       | 异步任务、消息队列             |
| 前端      | Vue3 + Element Plus  | 管理界面                       |
| 部署      | Docker Compose       | 一键编排，轻量运维             |
| 认证      | 钉钉 OAuth2          | 快速登录与用户同步             |
| 报表导出  | openpyxl / Pandas    | Excel 生成                     |





# 数据获取

数据源采集模块的目标，是在工程师通过VPN、SSH、堡垒机、向日葵、ToDesk等工具远程排查客户环境的过程中，自动捕获完整的操作记录，为后续Agent生成工单提供原材料。下面从实现方式、适用产品类型和部署方式几个方面展开说明。

------

**一、实现方式**

根据不同的远程接入手段，有四种可行的采集方案，通常组合使用效果最好。

1.  **对接现有堡垒机审计系统（最推荐）**
    如果企业已经部署了JumpServer、Teleport、云堡垒机等，这些堡垒机本身就具备会话审计功能，能记录SSH命令、回显、文件传输等。只需在堡垒机后台配置，让会话结束后自动把命令日志（通常是JSON或明文文本）推送到Agent微服务的Webhook地址，Agent解析后就能提取操作步骤。
    优点是不用改造客户服务器，数据格式统一；缺点是必须走堡垒机通道，绕过就无法记录。
2.  **终端录制工具（script/asciinema）**
    在目标客户服务器上，通过修改全局Shell配置（如/etc/profile），让用户登录时自动执行`script`或`asciinema`开始录制，把所有输入输出保存到日志文件，再写一个回传脚本定时把日志发回Agent服务。
    优点是可完整保留命令行操作，不依赖额外硬件；缺点是需要在每台客户服务器上做初始化，有侵入性，图形化操作也无法记录。适合命令行排障场景。
3.  **远程桌面工具的录像（辅助）**
    向日葵、ToDesk等工具通常提供会话录像功能，可以设置自动保存并回调通知Agent。但视频文件很难自动解析出操作步骤，比较实用的做法是直接将录像关联到工单里，供人工回溯，作为补充证据。视频转文字或多模态分析目前准确度有限，不推荐作为核心数据源。
4.  **自建运维代理网关（SSH中间人）**
    自己开发一个SSH代理，工程师所有SSH连接先指向这个代理，再由代理跳转到客户环境，代理同时旁路拷贝所有交互数据并结构化记录。
    优点是数据完全可控，缺点是开发维护成本高，需要推动工程师修改连接方式，只适合有较强研发能力且愿意接受流程变更的团队。

实际项目中，可以**主力对接堡垒机**，再提供一个“手动粘贴终端日志”的托底文本框，让工程师在特殊情况下把终端复制的内容贴进来，Agent同样能解析。这样用很小的成本就覆盖了绝大多数场景。

------

**二、处理流程适用的产品类型**

这个“远程操作→自动采集→生成工单”的流程，最适合那些部署在客户自己环境里、需要厂商工程师远程进去排查的产品。

1.  **私有化部署的B端软件**
    教育信息化系统（教务管理、智慧校园、在线教学平台）、医疗HIS、企业ERP等，部署在客户自有机房或私有云上。工程师必须通过VPN或堡垒机进去，所有操作天然产生SSH日志或后台操作日志，非常适合自动采集。
2.  **本地化部署的软硬一体机**
    比如智慧课堂一体机、录播主机、企业存储设备等，提供SSH或Web管理界面，维护过程同样可记录。
3.  **混合云/托管云的单租户实例**
    虽然跑在云上，但为了安全隔离，仍然需要通过跳板机或云堡垒机进入，操作审计同样适用。
4.  **需要远程桌面的客户端软件**
    比如桌面排课软件、考试客户端，工程师用向日葵/ToDesk直接操作用户电脑。这时虽然命令行记录不了，但可以利用工具自带的录像功能把操作过程存下来，作为工单附件辅助说明。

------

**三、适用的部署方式**

-   客户自有数据中心、校内机房，专线或VPN接入，走堡垒机最理想。
-   公有云独立VPC（阿里云、华为云），通过云堡垒机或跳板机维护，采集方案一致。
-   边缘节点或盒子设备，只要能提供SSH通道（哪怕是通过反向隧道），就可以用终端录制或代理方案。

**不太适用的场景**

-   纯多租户SaaS平台，工程师可以直接看统一后台日志，没有远程到客户专属环境的过程，这时候更依赖应用本身的日志系统，而不是终端会话采集。
-   完全靠电话指导客户操作，工程师端没有任何直接操作记录，只能事后手工补录。
-   客户环境安全策略严格禁止任何会话录制，就只能退回人工填单模式。

------

总的来说，数据源模块的关键是**和现有的远程运维通道做深度集成**，而不是另起一套复杂的录制系统。优先利用堡垒机这类现成的基础设施，配合简单的兜底手段，就能以较低的成本，为售后工单的自动化生成提供足够充分的数据基础，尤其适合以私有化部署为主的教育信息化产品。